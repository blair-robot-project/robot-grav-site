# FRC Team 449 Website — LIVE (robot.mbhs.edu) — Runbook
*Last updated: 2026-07-30*

Operational reference for the FRC 449 Grav site: environment facts, server housekeeping + security status, cautions/gotchas, and key file paths. For dated history of changes, see [CHANGELOG.md](CHANGELOG.md). For orientation, who's involved, and the doc to start a Claude Code session with, see [README.md](README.md).

---

## Repository & source of truth

This repo (`blair-robot-project/robot-grav-site`) is the source of truth for site documentation, held at its root.

- **Not a deploy source.** Nothing pulls from this repo to robot.mbhs.edu — no webhooks, no GitHub Actions, no `git pull` on the server. The live site serves local flat files on its own droplet.
- **Not a live content mirror.** The repo's `archive/user/pages/` is a frozen 2026-07-02 snapshot, not a live copy. Live backups run via Grav's own nightly scheduler, on the server itself.
- **Where secrets live:** not in this (public) repo. `CREDENTIALS.md` stays in the original private working repo. Server SSH access, the DigitalOcean console, and site secrets are held by Rafi (infrastructure) and Brad (project lead).

---

## Environments

| | **LIVE** robot.mbhs.edu |
|---|---|
| Role | Production — the only environment |
| Host | DigitalOcean droplet (nyc1), dedicated |
| OS / web server | Ubuntu 22.04, nginx 1.18.0 |
| Grav | **2.0.9** (admin2 **2.0.13**) — confirmed via `bin/grav --version` 2026-07-10; drifts via GPM, re-check before trusting |
| PHP | **8.3** |
| SSH | `ssh USER@robot.mbhs.edu` |
| Grav root | `/srv/robot-grav-site/` |
| Web user:group | `grav:editor` |
| MCP (Claude Code) | `grav-live` → `/api`, key on `bradP` |
| Admin | Admin Next (admin2 + api plugin) |

**Required config deviations from Grav defaults** (reapply if the site is ever rebuilt or re-migrated):
- `pages.markdown.gfm.tagfilter: false` in `system.yaml` — Grav's default GFM tagfilter escapes raw `<iframe>` tags, breaking every YouTube/video embed.
- ~~`text.html.twig` uses `image.height(N).html|raw`~~ — **stale as of 2026-07-29/30, superseded below.** The template no longer calls Grav's `.html()` helper at all (rewritten to build the `<img>` tag directly), so the `|raw`-autoescape concern this bullet described no longer applies. Left struck through rather than deleted so a rebuild doesn't reintroduce dead advice. See the `media_order` gotcha in Cautions & Gotchas for the current mechanism.
- An `/api/v1/sync` → 403 block in the nginx config — the `api` plugin doesn't implement these routes, and without the 403 they 404-flood and degrade the page editor.
- `image-intake` plugin must be **v0.5.0+** — earlier versions' gallery auto-sync silently stops working once the `api` plugin reaches ~1.0.3 (it now saves the page before firing its update event; v0.5.0 added a hook that handles this correctly).

---

## Access & Ownership

- **SSH hardened (2026-06-16):** root login disabled (log in as a normal user, then `sudo`); **fail2ban** bans an IP after repeated failed logins (don't lock yourself out testing — unban: `sudo fail2ban-client set sshd unbanip <IP>`; thresholds in CREDENTIALS.md). Password auth is still on (key-only is the optional next step). **Recovery / DO console:** the DigitalOcean web console logs in as any non-root user (then `sudo`) — so DO-dashboard access is root-equivalent, meaning the DO account needs a strong password + 2FA.
- Web + admin run as user `grav` (primary group `editor`). `USER` is in `sudo` + `editor`.
- **OWNERSHIP — the #1 gotcha.** Site files must be `grav:editor` so the web/admin user can write them. If you create/overwrite files as `USER` (scp, rsync, `>` redirects), `chown` them back: `sudo chown -R grav:editor <paths>` (exclude `.git` — its index is root-owned for the backup cron). Symptoms of getting it wrong: admin "Failed to save," or a 500 after a plugin op.

---

## Server housekeeping — live (robot.mbhs.edu)

**Disk:** 25 GB total, ~80% used, ~5.1 GB free (as of 2026-07-28). Tighter than the 2026-07-05 figure below — grown with site content since, and a since-fixed backup bug (see Git Sync section) was briefly doubling nightly backup size on top of that. Backup retention was just cut 14→5 and the bug fixed; expect this to improve over the next several nights as oversized old backups roll off, not retroactively.

- [ ] **SSH hardening — partially done.** fail2ban active, root login disabled. **Still open:** `PasswordAuthentication` is still `yes` in `sshd_config` — switching to key-only auth would close the remaining gap (verify a new key login works before disabling).
- [ ] **Grav admin 2FA — still 0 of the active accounts have TOTP enabled.** `/admin` is internet-reachable; enabling 2FA at least on super-admin accounts is the recommended next step.
- [ ] **MariaDB running but apparently unused** (Grav is flat-file; bound to 127.0.0.1). Confirm nothing depends on it, then `sudo systemctl disable --now mariadb` to free resources and reduce surface area.
- [ ] **No Content-Security-Policy header.** Optional defense-in-depth; would need per-page testing before enabling.
- [ ] **(Optional) Restrict `/admin` reachability** — IP-allowlist or HTTP basic-auth in front of it, for defense-in-depth beyond account-level 2FA.
- [ ] **Rotate/remove the old root SSH deploy key** (`/root/.ssh/id_ed25519`, still the default `Host github.com` identity in `/root/.ssh/config`) — leftover from the now-retired `backup.sh` mechanism (see Git Sync section), still has push access to this public repo. Rafi's action — needs root.

**Already done, no longer open (see CHANGELOG for dates):** nginx TLS/headers/server_tokens hardening; stale-account pruning; nginx blocklist gap that let `user/data/` runtime files leak (fixed); root-level `.md` file disclosure (fixed); ImageMagick MVG/MSL policy hardening; journal size is healthy (56 MB, no vacuum needed); the 1.7 archive, the loopback-only test vhost, and the unused PHP 8.0/8.2-FPM pools have all been retired (2026-07-05).

### Live large-photo upload limits (4-layer config — not in the Grav backup)
Smallest layer wins; all four are raised on live so 9-12 MB phone photos upload successfully:

| Layer | Value | Where |
|---|---|---|
| nginx `client_max_body_size` | 32M | HTTPS server block, `/etc/nginx/sites-available/grav` |
| PHP `post_max_size` | 30M | `.user.ini` at the Grav root |
| PHP `upload_max_filesize` | 25M | same `.user.ini` |
| Grav `system.media.upload_limit` | 25M (26214400) | `user/config/system.yaml` |

`memory_limit` is deliberately left at 128M — `image-intake` resizes via a `convert` subprocess, so this is safe. **`.user.ini` and the nginx setting live outside `user/`, so neither is in the Grav backup** — reapply both if the droplet is ever rebuilt (and clear compiled config after).

---

## Architecture reference

**Modular templates** (`templates/modular/NAME.html.twig`):
- **feature-images** — image grid, optional per-item link + lightbox; resolves images via `page.media`.
- **icon-menu** — icon + label + link "nav cards" (frontmatter key stays `features:`).
- **gallery-draggable** — photo gallery; display order = the admin Page-Media drag order (`page.media.images`); click → lightbox.
- **gallery-banners** — single shrink-to-fit row, no lightbox, no rendered title; same `page.media.images`-order pattern as gallery-draggable. Home page's 13-banner Blue Alliance trophy history (added 2026-07-10, ported from staging same day). Each banner can optionally link to its real thebluealliance.com event page via an admin-editable `banner_links` field (image + URL per row, in the module's "Banners" tab) — verify new links against the real TBA API, not guesses (key in `bpeniston/449-website`'s `CREDENTIALS.md`).
- **gallery-press** — press-kit photo grid: no crop (true aspect ratio, unlike gallery-draggable), full-resolution downloads, per-photo caption + credit via a `photo_meta` list matched to each image **by filename** (reordering Page Media never desyncs captions). Ported from staging 2026-08-12, first live instance is `/community/summer-classes` (empty scaffold — no photos yet, pending content). Requires `caps.gallery-press` in `image-intake.yaml` to be a real width, not `0` — `0` silently skips both the resize *and* the metadata-strip that rides the same ImageMagick call (GPS/EXIF/IPTC/XMP), a bug caught and fixed on staging 2026-08-09. Currently `4000` on both sites.
- **text**, **hero** — lightly customized standards. **footer-col** — minimal footer-column wrapper.

**Partials:** base (head/scaffold + the `?v=` cache-bust), footer (renders the `/footer` page's modules), lightbox (no-JS clickable thumbnail), error (the branded 404), **announcements** (site-wide dismissible announcement bar, added 2026-07-09, renamed from `banners` 2026-07-19 to disambiguate from the `gallery-banners` module above — see below).

**Admin-editable "list" data pages** (not modules — a single hidden page with a repeating list field, for content that's a growing collection rather than a one-off block): **sponsors-data** (sponsor roster, synced from a Google Sheet via webhook) and **announcements-data** (announcements — title/message/link/color/pages/dates per entry, see `INSTRUCTIONS.md` §5). Both read via `page.find(route, true)` from the template that needs them, not the `pages` global — non-routable pages aren't visible to `pages.find()` in this Twig build. **`sponsors-data` is staging-only (449.navybook.com)** — built and working there, but deliberately not yet deployed to live, pending a decision from the team's business side on whether/when to bring it over (confirmed with Brad 2026-07-22). `announcements-data` is live on both.

**Images:** PHP gd + the ImageMagick `convert` binary. The custom image-intake plugin sanitizes filenames + shrinks photos on upload (per-template max widths, set in Admin → Plugins → Image Intake).

A fuller template reference (including page-level templates and admin-selectability) lives in `INSTRUCTIONS.md`'s appendix.

---

## Git Sync (added 2026-07-28)

The official Git Sync plugin syncs `user/pages` + `user/themes` with a dedicated private repo, **[`blair-robot-project/robot-grav-site-sync`](https://github.com/blair-robot-project/robot-grav-site-sync)** — deliberately not this repo, which is hand-curated docs, not an automated content mirror. Fires in real time on every admin save/delete/media change (`on_save`/`on_delete`/`on_media`), and no inbound webhook (the plugin's own webhook toggle stays **Disabled** — see the gotcha below on its misleading label).

- **⚠️ It is two-way, and it IS on a cron — so the sync repo can write to live.** The enabled **`git-sync` scheduler job at `0 0 * * *`** means anything committed to `robot-grav-site-sync` is pulled onto the live site at the next midnight, with no review step and no warning at commit time. *(Corrected 2026-07-31: this section previously said "no cron." It was wrong — `bin/grav scheduler -d` lists the job Enabled, last run 2026-07-31 00:00, Errors: None.)* Treat a push to that repo as a deployment, not a backup. Confirm current state with `grep -A6 '^sync:' user/config/plugins/git-sync.yaml` and `sudo -u grav php bin/grav scheduler -d`.

  Three things that are easy to get wrong here — all verified against the plugin source on live (`user/plugins/git-sync/classes/GitSync.php`, `classes/Helper.php`):

  - **The disabled webhook does NOT make this safe.** `webhook_enabled: false` removes only the *instant* delivery path; the scheduled `Helper::synchronize()` → `GitSync::sync()` is a second, independent inbound route. The reasoning "the webhook is off, therefore the sync repo cannot reach live" has been drawn before and is wrong. (It may have been *true when drawn* if the cron was enabled later — but it is not true now.)
  - **`direction` does not gate the pull.** In `sync()`, `fetch()` and `pull()` run unconditionally; the `direction: both` check wraps only the `push()`. So you cannot make the site inbound-safe by changing `direction` — the only off switch is `cron_enable: false`.
  - **The pull prefers the remote.** It runs `git pull --allow-unrelated-histories --ff -X theirs <remote> <branch>`, so on conflict the remote copy **overrides** what is on the live server, rather than failing or keeping local.

  Why the reflog shows no pulls: every commit in that repo so far originated from live's own admin saves, so there has never been a remote-only commit to pull. The absence of pull activity is not evidence the mechanism is inert — it has simply never been exercised.

- **To see what changed and when:** the repo's [commit history](https://github.com/blair-robot-project/robot-grav-site-sync/commits/main) — click any commit for the exact files touched. Commit messages are generated by Claude (a `post-commit` hook, added 2026-07-30) rather than git-sync's generic default, and are prefixed **`(GitSync) <route> by <who>:`** (e.g. `/about-us/history/_frc-2015`) so the commit list alone shows which page changed and who changed it — the route, not the file path, because it survives folder renumbering; theme files, having no route, keep their path — see the 2026-07-30 CHANGELOG entries for the mechanism and the credential (a disposable, spend-limited API key, not a personal one).
  - **⚠️ Testing a git hook here: use the admin panel/API, never a command-line `git commit`.** **PHP-FPM runs hooks with `HOME` empty**, but a shell (including `sudo -u grav`) sets it — so anything in a hook that derives a path from `$HOME` works from the command line and **silently does nothing on every real admin save**. This hid a completely dead hook on staging for three days, with no log output, because the log path was `$HOME`-based too. Derive paths from the script's own location (`BASH_SOURCE[0]`), not `$HOME`. Tell a real test from a false positive by the commit's author: **`GitSync <git-sync@trilby.media>`** = a command-line commit (that name is the repo's own local git config), **`bradP`/`Admin`** = a genuine admin save. *(`sudo -u grav` is still correct for **placing** files under `/home/grav/` — just not for testing.)*
- **`config`/`plugins`/`data` are deliberately excluded** from the sync scope — `user/config/` holds secrets, including this plugin's own GitHub token (`user/config/plugins/git-sync.yaml`). Never add those to the Folders setting.
- **Auth is a fine-grained PAT on the `blair-robot` account** (Contents: Read & write, scoped to only this one repo) — not tied to any individual's personal GitHub account, so it keeps working through personnel turnover. The account's GitHub password is held by team leadership.
- **A whole-site git mechanism predates this and is now retired** — `/srv/robot-grav-site/.git.retired-20260727` (renamed, not deleted; full history preserved on GitHub under this repo's own `archive/grav/`). It was a 2022-era root-cron script (`backup.sh.disabled-20260702`), unrelated to Git Sync, disabled during the 2.0 migration and forgotten until this work rediscovered it. **Still open:** its root SSH deploy key (`/root/.ssh/id_ed25519`) is still the default `Host github.com` identity in `/root/.ssh/config` — needs rotation/removal by Rafi (Git Sync itself doesn't use it — HTTPS + PAT, not SSH).
- **This directory caused a real backup bug, since fixed** — see Server Housekeeping's disk note above and the 2026-07-28 CHANGELOG entry. If `.git.retired-20260727` is ever fully deleted (not just excluded), no further action needed here; if it's ever renamed again for any reason, update the backup profile's `exclude_paths` to match the new name or the same bloat recurs.

### Gotcha: the webhook toggle is mislabeled in the plugin's own UI
Two adjacent, near-identically-named fields: the toggle that actually enables/disables the webhook is labeled **"Web Hook Secret"**; the actual secret text lives in a separate field called **"Repository Web Hook Secret."** The one to keep **Disabled** is the toggle, not the text field. This is a plugin bug, not a documentation gap — confirmed against the plugin's own blueprint source.

---

## Cautions & Gotchas

- **Only edit `mod-quark`, never the parent `quark` theme.** Mod Quark (`user/themes/mod-quark/`) is a custom child of stock Quark (`user/themes/quark/`).
- **A custom modular template needs a matching blueprint.** New `templates/modular/NAME.html.twig` requires `blueprints/modular/NAME.yaml` (`@extends: default`), or the admin **silently overwrites your template** to a stock type when the module is saved. (Why feature-images / icon-menu / gallery-draggable each have one.)
- **Protect custom theme blueprints before any theme update.** Copy the theme first (`cp -r user/themes/mod-quark user/themes/mod-quark-backup-$(date +%Y%m%d)`), then diff afterward to confirm nothing custom got overwritten.
- **Mod Quark is a custom Git repo — not GPM-managed.** Not installed through Grav's package manager; `bin/gpm update` may not handle it. Manage manually via SSH/GitHub. **Plugin/Grav GPM updates: only ever as `grav`** (Admin "Update," or `sudo -u grav php bin/gpm update`) — never as `USER` (it half-installs and breaks admin). **Don't remove the `email` plugin** — it's a required admin dependency (admin 500s without it), even though the site has no Grav forms (contact is `mailto:` links).
- **Update one thing at a time.** Update a single plugin/theme, verify it works, then move to the next. Never chain updates — if something breaks, you want to know exactly which change caused it. Take a fresh Grav backup right before any plugin/theme update.
- **Link portability — never hardcode paths.**
  - Page-media images: filename only. `![](filename.jpg)`.
  - Shared images (`user/images/`): `![](/user/images/x.jpg)` — Grav auto-prepends the base for markdown, but **not** for raw HTML `<img src>` — for raw HTML, add `process: { twig: true }` and use `src="{{ base_url }}/user/images/x.jpg"`.
  - Internal page links: root-relative, no domain — `[text](/about-us/leadership)`.
  - Frontmatter URLs rendered via Twig (e.g. `icon-menu`/`feature-images` `url:` values): Grav does **not** auto-prepend the base to these — the template must (existing menu templates already do). Any new module emitting a frontmatter URL through Twig needs the same treatment.
- **Admin `</>` raw-source toggle is broken** — you can't edit frontmatter via the WYSIWYG. Use SSH (or admin Expert Mode) for frontmatter; normal body text edits in the WYSIWYG work fine.
- **Notes & comments conventions.** Grav has no dedicated notes field:
  - Every new folder/page/module starts its Content field with `[//]: # (CommentsGoHere)` — a blueprint default (`mod-quark/blueprints/default.yaml`) every page/module inherits via `@extends: default`. Replace with a real note when there is one.
  - A note a teammate should see → `[//]: # (your note here)` — renders nothing, visible in the editor.
  - A "how this folder works" note → a sidecar `_NOTES.md` file (Grav ignores non-template files).
  - Avoid `<!-- HTML comments -->` for anything non-trivial — unlike `[//]: #`, they ship in rendered HTML.
- **Uploads:** image-intake auto-shrinks on upload. Size limits are already set across all 4 layers (see Server Housekeeping above); `memory_limit` is 128M (resize runs in the `convert` subprocess, so it's safe).
- **Grav `api` plugin keys: paste only the bare `grav_...` token.** Generating a key (`bin/plugin api keys:generate`) prints a line like `API Key: grav_...`; copying more than the token itself into a `claude mcp add --env GRAV_API_KEY=...` command splits on the extra words and silently corrupts the registered command's args. Select just the token (double-click it) before pasting.
- **Creating a brand-new theme file over SSH needs one `sudo` step first.** An SSH account that's only in the `editor` group (not `grav` itself) can edit *existing* `664` files fine, but the theme directories aren't group-writable, so it can't create a new file directly. Fix: `sudo -u grav touch <path> && sudo chmod 664 <path>` once, then write the content over plain SSH/SCP — no further `sudo` needed. The `api` plugin's `upload_blueprint_file`/`blueprint-upload` endpoint looked like a no-sudo alternative (it runs as `grav`) but explicitly rejects `.yaml` destinations for security (`grav.log`: "File extension '.yaml' is not allowed for security reasons") — it's scoped to actual blueprint-referenced uploads (images etc.), not general-purpose file creation. Used this pattern to deploy the announcements feature's new files (2026-07-09).
- **Admin Next's `list` blueprint field has no per-item styling or multi-field summary hooks.** `collapsed: true` on the field is a real, supported option (rows load collapsed to one line instead of always-expanded — worth setting once a list is expected to grow). But the collapsed row's label is hardcoded client-side to "the first non-empty string field's value" — no config for color-coding rows by a field value, or combining multiple fields into the summary. Confirmed by reading the compiled admin2 JS bundle (`user/plugins/admin2/app/_app/immutable/chunks/*.js`); there's also no `custom_css`/`custom_js` injection point for this SPA (unlike the classic admin plugin's `onAdminCss` hook, which doesn't apply since admin2 is a separately-built static app, not Twig-rendered). Workaround: put whatever you want visible collapsed into the first field itself (e.g. dates folded into a title field) rather than expecting it to be computed.
- **A fixed-position `#header` needs deliberate top-offset handling for anything inserted above the hero.** See `INSTRUCTIONS.md` §11 for the specific rule (`position: absolute` + explicit `top`, never `margin-top`/normal flow) — pushing the hero down to make room breaks the header's see-through look, since it depends on the hero rendering directly behind it.
- **The `grav-live`/`grav-staging` `api` plugin can be called directly over plain HTTP, without Claude Code or MCP at all** — useful when something needs to run on a schedule/machine Claude Code itself can't cover (e.g. a 24/7 always-on box that isn't this laptop). Base URL `https://<host>/api/v1`, header `X-API-Key: <key>` (same key already registered for the MCP connector), `GET`/`PATCH /pages/{route}` (send `If-Match: <etag>` from a prior `GET` for optimistic concurrency). **`header` fields — including list-type fields — are replaced wholesale on `PATCH`, not deep-merged element-by-element:** round-trip the *whole* list back if only one entry actually changed, or the others get silently wiped. Full contract: `~/Documents/449/grav-mcp/src/client/grav-client.ts` in the `grav-mcp` connector's own source. First used 2026-07-11 for a standalone TBA-driven banner-update script running off this repo's server entirely (see CHANGELOG) — that script also hit a Cloudflare block on the *TBA* side (not Grav's): TBA's WAF rejects Python's default `urllib` User-Agent (403), fixed by sending a `curl`-like one. **Stale after the 2026-07-19 announcements rename:** that script (`~/robojawn-449-banner/banner_watch.py` on Brad's Air, dormant since its bounded July 11–12 event window closed) hardcodes the old `/pages/banners-data` route and `banners` header field — it 404s as-is and needs updating to `/pages/announcements-data`/`announcements` before any future reuse.
- **AppleScript/iMessage automation from a headless Mac needs an unlocked, actively logged-in GUI session — the process running isn't enough.** `tell application "Messages" to send ...` hung with `AppleEvent timed out (-1712)` when the target Mac's screen was locked, even though Messages/`imagent` were already running; confirmed via a failed `screencapture` ("could not create image from display"). Unlocking the screen (and approving the one-time Automation permission prompt) fixed it immediately. Used for the RoboJAWN banner watcher's text-on-update feature (2026-07-11, see CHANGELOG) — if the screen re-locks, texts silently stop while whatever else the automation does keeps working.
- **`text.html.twig`'s image loop needs both a `media_order` fallback and a per-item `{% if image %}` guard — don't remove either.** The loop resolves each image by name: `{% set _names = page.header.media_order ? page.header.media_order|split(',') : page.media.images|keys %}`, then `page.media[image_name]` per entry.
  - **No `media_order` field at all** → falls back to `page.media.images|keys` (Grav's automatic list). Without this, a page that's never had `media_order` set — true for any single-image page nobody has ever manually reordered in the admin Page Media panel — silently renders no image at all. Found live 2026-07-30 on 3 of the 86 pages using this template (`41._history-1999`, `05.CURRENT-SPONSORS/15._intuitive`, `05.SPONSOR-BENEFITS/05._text-block-c`), after `media_order`-based lookup replaced the old `page.media.images`-loop approach on 2026-07-29 (see CHANGELOG).
  - **A `media_order` entry naming a file that no longer exists** (stale reference after a manual delete/rename, `media_order` not updated to match) → `page.media[image_name]` returns null for just that one entry. Everything derived from it — the `-original`/`-hover` lookups, the `<a>`/`<img>` tags — sits inside `{% if image %}`, so that one entry is skipped cleanly instead of rendering a broken `<img src="">`. Found live the same day on `33._frc-2008` (`media_order` still listed a file deleted 2026-07-30 morning); fixed via `mcp__grav-live__update_page`, not SSH (page content files are `644` — see Access & Ownership; only theme files are the group-writable `664`).
  - **Also worth knowing:** the size cap that used to sit here (`.height(N)`, capping every image to a fixed height) was removed 2026-07-23 to fix Past Presidents photos rendering at inconsistent widths (fixed-height scaling makes width vary across differing aspect ratios) — but removing the cap entirely, rather than switching its anchor to width, means every image on all 86 pages sharing this template currently renders at full original resolution, uncapped. Not yet fixed as of 2026-07-30 — intentionally left as a task for whoever picks it up next rather than pre-solved.
- **Grav's `Medium::html()` puts `$title` before `$alt`** — full signature `html($title = null, $alt = null, $class = null, $id = null, $reset = true)`. Easy to get backwards: most people instinctively expect alt first, since it's the attribute that actually matters for accessibility/SEO, title is just a tooltip. Pass one positional arg meaning it as alt text and it silently lands in `title` instead, leaving `alt=""`. This is Grav behaving correctly and as documented, not a bug — confirmed against Grav's own core source and a closed upstream issue (`getgrav/grav#1253`) establishing that Grav can't invent alt text for you, so it deliberately emits valid-but-empty `alt=""` when nothing is passed rather than guessing. Bit three separate Mod Quark templates this way before a full-site alt-text audit caught and fixed all of them (`gallery-banners.html.twig`, `gallery-draggable.html.twig`, `text.html.twig`; 2026-07-18/19, see CHANGELOG). **Any new image-rendering template: pass both, explicitly, in order** — `image.html(alt_text, alt_text)` sidesteps the ordering risk entirely by using the same value for both.

### Rollback options (live)
- **Whole-site: Grav's own nightly scheduled backup** (5-copy rotation as of 2026-07-28, down from 14 — see Git Sync section; `backup/` folder) — `php bin/grav restore <zip>`, then `php bin/grav clearcache`.
- **Single page/theme file: Git Sync's history** (see Git Sync section above) — browse or check out an older version of one file from `robot-grav-site-sync` on GitHub without restoring the whole site.
- **Admin-panel changes** (plugin/theme config) can usually be reverted through the admin panel — the safest option.
- **Do not attempt PHP/system-level rollbacks yourself** — contact Rafi Pedersen for that level of recovery.

---

## Lessons for a future platform migration

The 1.7 to 2.0 migration (completed 2026-06-27) is done and its environment-specific detail has been retired along with the 1.7 archive itself. The durable lessons, if a future migration ever happens:

- **Parallel-build-then-swap beats an in-place upgrade** on a production box: build and fully test the new version on a copy with zero visitor impact, then cut over via a directory swap (fast, trivially reversible) rather than upgrading in place.
- **A migration/distribution tool that swaps in a "fresh" install can silently drop non-distribution files** sitting at the docroot root (upload-limit configs, `robots.txt`, verification files, `.git`) — anything outside the tool's own file manifest. Inventory and re-copy these explicitly; don't assume they survive.
- **Verify rendered behavior, not just HTTP 200s.** Version-skew between a test copy and a fresh install pulled by a migration tool can introduce regressions that only show up by actually looking at pages with images/embeds, not by checking status codes.
- **New installs created by migration tooling may have different default file ownership** than the established convention — check and fix before deploying custom carry-forward files into them.
- **Get a full site backup and confirm it off-server before starting**, and keep the pre-migration environment available for a real soak period (roughly a week) before removing it — but do actually remove it once the soak period is over, rather than let it linger indefinitely as silent disk/attack-surface debt.

---

## Mod Quark -> Quark 2 migration — IN PROGRESS, and gated on an open decision (see below)

**Status: filed 2026-07-21. Phases 0-2 done 2026-07-22. Phase 3 (verify) is STILL NOT COMPLETE as of 2026-09-08** - it was reported "fully complete" on 2026-07-23 and twice more after that, each time prematurely; 28 numbered regressions have been found since. Nav/hero/typography and the icon-menu module are now genuinely exact against live, and headings h2-h6 are verified across three pages, but body typography beyond headings has not been checked at all and neither has most of the site. **Do not treat this section's later "done" claims as load-bearing without re-measuring.**

---

### ⚠️ OPEN DECISION (raised by Brad 2026-09-08): should we ship mod-quark2 at all?

**Nothing below this line should be treated as settled until this is answered.** The remaining Phase 3 work is pixel-mirroring, and if the answer here is "stop mirroring," most of it is moot.

**The question Brad asked:** we can clearly *build* a Quark 2-based theme that mirrors today's Mod Quark closely — that is what has been happening. But should we ship it? Specifically: does insisting on mirroring create traps or extra work **on the other side**, for the high schoolers who maintain this site, who are not web experts and are more interested in their robots? Brad was explicit that build cost is not a factor in this decision; only downstream maintenance cost is.

**The evidence, measured rather than asserted:**

| | Lines | `clamp()` formulas |
|---|---|---|
| live `mod-quark/custom.css` | 594 | 0 |
| ported `mod-quark-2/custom.css` (v33) | **889** | **29** |

To make Quark 2 *look like* Quark 1 we have written half again as much CSS as Quark 1's entire custom layer, and essentially all of the excess exists to undo Quark 2's own design decisions:
- `.section.modular-features { padding-block: 0 }` — cancels Quark 2's section rhythm (item 21)
- `:is(p,ul,ol,…) + :is(h2,h3,h4) { margin-top: … }` — cancels Quark 2's contextual heading rhythm, restated at matching specificity purely so it wins (item 26)
- `--pico-line-height: 1.5` **plus** `p,li {1.5}` **plus** `.feature-content p {1.5}` — three separate places to undo one typographic choice (item 24)
- Full restatements of the `h2`/`h3`/`h4`/`h5` type scale and weight (items 24-27)

**Why that is a maintenance trap specifically.** A formula like `clamp(22.4px, 19.04px + 0.7vw, 28px)` means "1.4rem of Spectre's fluid root font-size." Nothing in the file makes that legible to a student: to change a heading size safely they must know Spectre's root formula, re-derive three numbers, and understand why a plain `1.4rem` will not work. They would also need to hold **two** design systems in their head — Spectre's, to understand why an override exists, and Pico's, to understand what it is overriding. That cost is permanent and recurring, and it is created by the mirroring requirement, not by Quark 2.

**Why it is also fragile.** Every override is a bet that Quark 2's internals stay put, and Quark 2 is actively maintained via GPM. A changed selector shape upstream means a matching-specificity override silently stops matching. *Silently* is the operative word — the same failure mode as the `*/`-in-a-comment bug (item 5, dead rule for weeks) and the `h2` fix that looked exact on the homepage while being wrong on 25 headings elsewhere (item 26). Both were caught only by measuring obsessively across multiple pages. A student maintainer will not do that.

**The three options:**

1. **Ship the pixel-perfect mirror** (current trajectory). Maximum visual continuity; highest ongoing maintenance cost, most fragile under upstream updates, hardest for a non-expert to reason about.
2. **Ship Quark 2 but drop the mirroring requirement.** Keep the module ports (`icon-menu`, `feature-images`, `gallery-draggable`, `gallery-banners`, `gallery-press`, `footer-col`, the `text`/`hero` modifications and blueprints — 449-specific functionality that must exist under any theme, and the bulk of the real engineering) and keep 449's identity (the red, logo, nav structure, hero-behind-transparent-header, Inter/Science Gothic). **Throw away** the typography/rhythm/type-scale mirroring — most of the 2026-09-08 work. The site visibly changes; someone must sign that off.
3. **Stay on Quark 1.** The site works. Spectre is dormant rather than broken. Best maintenance profile of all, right up until something forces the issue (a PHP/Grav change, or an upstream fix that never comes).

**Claude's recommendation (2026-09-08), for the team to accept or reject:** option 2. The strongest supporting evidence is in this repo's own CHANGELOG — **live's typography changed on 2026-09-04, mid-migration.** The team is evidently willing to change how the site looks, which suggests the real requirement is "looks like us," not "matches the old CSS pixel-for-pixel." Mirroring a target that moved twice in seven weeks is a treadmill with no finish line. Option 1 optimises for a constraint the team has already demonstrated it does not hold.

**If option 2 is chosen, write the rule down**, because "mostly defaults with a few exceptions" degrades back into option 1 without one: *overrides are allowed for identity (color, logo, custom modules) and forbidden for type scale, spacing, and rhythm.*

**What is NOT in dispute either way:** the module/template/blueprint ports are worth keeping under options 1 and 2 alike, and the verification lessons below (totals are a worthless parity metric; one page is not enough; a CSS-variable change may not propagate; scan before measuring) apply to any future theme work here.

#### Option 2 previewed and measured, 2026-09-08 — what it actually looks like

Rather than argue this in the abstract, option 2 was built and viewed. **`user/themes/mod-quark-2/css/custom-option2.css` is on the test copy** and stays there for future comparison. To view it, point the `addCss` line in `base.html.twig` at `custom-option2.css` instead of `custom.css`, clear cache, and **`touch` the template** (see the mtime gotcha in the list above); switch the filename back to restore the mirror. The test copy is back on the v33 mirror — the preview was temporary.

It was generated by rule, not by taste: **keep anything a custom module needs to function or that carries 449 identity; drop anything whose only purpose is making Quark 2 measure like Quark 1.** So the `.columns` flex fix (Blades genuinely needs it), grid widths, box-sizing, the red header/footer, logo, announcement banners, galleries, year/history modules, branded 404, the Inter + Science Gothic fonts and the h1 stroke/uppercase all stay. The type scale (h2-h5), heading-rhythm overrides, line-height overrides, paragraph spacing, section/container padding mirrors, icon sizing and palette, nav typography and the Spectre 976px/600px width mirrors all go.

**The headline result:**

| | Mirror (v33) | Option 2 |
|---|---|---|
| `clamp()` formulas | **29** | **0** |

Every hand-derived fluid-math formula disappears — which was the whole maintainability objection.

**How it looks.** Hero: red header, Science Gothic uppercase title with stroke, all intact, and the h1 becomes *fluid* (Quark 2's `clamp(2.25rem, 5vw + 1rem, 3.75rem)`) instead of locked at `50px !important` — genuinely better on small screens than what live does today. Icon-menu: becomes Quark 2 cards (white panels, small dark icons top-left, centred links) - completely unlike live's big-pale-grey-icon-behind-text treatment, but coherent, not broken. Sponsors: clean card grid with logos and italic tier labels, arguably reading better than the current version. History page: Science Gothic hero, Inter body copy, legible throughout.

**Two real regressions, found by measuring rather than eyeballing** — these are the honest cost:
1. **Line length.** Body copy runs **1136px wide** (~140 characters per line) on `/about-us/history`. Well past the readable range; live constrains it.
2. **Hyphenation.** Quark 2 sets `hyphens: auto`, producing "host-ed", "an-other", "back-to-back" mid-paragraph. Live does not.

**So the fair comparison is not "29 rules versus 0."** It is **29 mirroring formulas versus roughly two or three deliberate design decisions** - a prose max-width and a hyphenation choice, each a single readable line a student could understand and change:
```css
.modular-text .column { max-width: 70ch; }
p { hyphens: none; }
```
That still favours option 2 heavily, but it should be argued on those terms rather than on a zero.

**Caveat on the generated file:** its comments are stripped, so **do not read its line count as meaningful and do not ship it as-is.** If option 2 is adopted, rebuild the stylesheet deliberately from the mirror, keeping the explanatory comments; the generated file is a look-and-feel preview only.

**Read this before touching Phase 3+:**
- **Ownership: the test copy is `grav:editor` as of 2026-09-08** (it was deliberately `brad:editor` during Phases 1-2 for iteration speed, then flipped). The live trap now is the reverse: **any `scp`/`cp`/`sed -i` you run as `brad` recreates a file under your own ownership**, so check `stat -c '%U:%G'` after editing and `sudo chown grav:editor` back. Re-apply `chmod -R g+w` on `cache/logs/tmp/images/assets/user/{accounts,data,config,pages}` if ownership ever moves wholesale - Grav's Problems plugin shows a "not writeable" diagnostic page instead of the site if this is missed (cost real time twice in Phase 1).
- **Verification tooling - this advice was REVERSED on 2026-09-08, read the current version.** The original 2026-07-22 note said JS `getComputedStyle()` reads were unreliable and screenshots were the only trustworthy check. That was true of the **in-app Browser pane** (`mcp__Claude_Browser__*`), which also returns blank screenshots after a JS-driven scroll. It is **not** true of **Claude in Chrome** (`mcp__claude-in-chrome__*`), where computed-style reads were accurate and load-bearing throughout the 2026-09-08 session - essentially every fix in items 21-28 was verified that way, and screenshots served only as a sanity check. **Use Claude in Chrome, and trust per-element computed styles; but see the four verification lessons in the Phase 3a/24/26/27 entries - measure per element (never totals), on more than one page.**
- **Restoring a template with `mv` from a `.bak` leaves Twig serving the stale compiled copy, and `bin/grav cache` will NOT fix it.** `mv` preserves the backup's original mtime, so the restored file looks *older* than Twig's compiled cache and never gets recompiled - the page keeps rendering the version you just reverted, with the on-disk file provably correct, which is maximally confusing. **`touch` the template** after any `mv`-based restore. (Cost real time during the 2026-09-08 option-2 preview swap. Deleting `cache/twig/*` by hand does not work either: those files are owned by `grav` and `brad` cannot remove them.)
- **`custom.css` needs its cache-bust version bumped by hand on every edit** (`theme://css/custom.css?v=N` in `base.html.twig`) - it's served `Cache-Control: immutable, max-age=1yr`, so without a version bump, browsers (including whoever is checking the tunnel) silently keep serving the pre-edit copy. **Currently at `v=33`** (2026-09-08). Note a `sed -i` or `scp` as `brad` recreates the file and leaves it `brad:editor` rather than `grav:editor` - check and `chown` after, see the Phase 1 ownership note.
- **The Phase 0 backup (`default_site_backup--20260722071456.zip`) still hasn't been copied off-server** - Brad's doing this manually next time he's on non-cellular wifi, not blocking anything.

**Read this before touching `custom.css` or any Blades-based template again:**
- **Blades' `.columns` class is CSS multi-column *text* layout** (`columns: 25ch auto`), not a flex/grid row like Spectre's `.columns`/`.column`. Every grid module (`icon-menu`, `feature-images`, `text`/year-modules) needed `columns: unset; display: flex; flex-wrap: wrap;` re-added, scoped per module, to avoid clobbering Quark 2's own use of real multi-column layout elsewhere.
- **Blades has no global `box-sizing: border-box` reset** the way Spectre did - percentage-width columns with padding silently overflow their container and wrap onto a new line instead of sitting side by side. Added `box-sizing: border-box` explicitly wherever percentage widths + padding combine.
- **Quark 1 and Quark 2 use different scroll-detection mechanisms**: Quark 1's JS toggles `.scrolled` on `#header` itself; Quark 2's toggles it on `<body>`. Any old `#header:not(.scrolled)`-style selector ported verbatim will silently never detect scroll under Quark 2 - rewrite as `body...:not(.scrolled) #header`.

**Strategy:** same pattern as the 1.7->2.0 core migration - parallel build, verify, then cut over. Never an in-place theme swap on live.

### Phase 0 - Recon and backup (no live changes) — done 2026-07-22
- [x] Confirm current Grav/admin2/API versions, take a full site backup first. **Live:** Grav 2.0.12, Admin2 2.0.15, API 1.0.12, PHP 8.3.31. Backup `default_site_backup--20260722071456.zip` (367 MB) taken via the API plugin - 14th in the 14-count/5 GB-capped rotation (4.9 GB used, already at the policy ceiling); **not yet copied off-server**, unlike the general migration prerequisite above.
- [x] Inventory everything non-stock in Mod Quark: templates (`icon-menu`, `feature-images`, `gallery-draggable`, `gallery-banners`, `footer-col`), partials (`base`, `footer`, `lightbox`, `logo`), `error.html.twig` overrides, all of `custom.css` (479 lines), the `announcements-data` page, the disabled BlairMdITC `@font-face` block (still present, still commented out, `custom.css:126-148`). **`sponsors-data` does not exist on live** - built and working on staging only, deliberately held back pending a decision from the team's business side on whether/when to bring it to live (confirmed with Brad 2026-07-22; Architecture Reference entry above corrected to match) - nothing to inventory on live for this yet. **Found but not previously documented anywhere** - confirmed genuinely modified by diffing against stock `quark`, not just present by name: `templates/modular/features.html.twig` (adds a `text_align` style hook), `templates/macros/macros.html.twig` (reworked nav - active-class handling + a `notclickable` non-link menu-item mode), `templates/forms/fields/radio/radio.html.twig` (fallback for a custom `tu` translation filter). Also: 16 stray `.bak-*` files scattered live inside `mod-quark/` (dated 06-15 through 07-02) - clutter worth knowing about when diffing against stock, left alone.

**Quark 2 clarified (2026-07-22):** it's a real, separate GPM theme - [`getgrav/grav-theme-quark2`](https://github.com/getgrav/grav-theme-quark2), slug `quark2` - **not** a version bump of the `quark` package already on live (that one's still v2.1.4, Spectre-based, legacy, and is a red herring for this plan). `quark2` is currently **v1.1.6** (13 releases since 1.0.0 on 2026-04-15 - pin a specific version in Phase 1 rather than always-latest, and re-check right before Phase 1 actually starts). Its README confirms the plan's assumptions below were right all along: Blades CSS (Pico's maintained successor, `--pico-*`/`--q2-*` custom properties), Font Awesome 7, Cal Sans/Inter fonts, sticky/animated header, and a built-in accent-color + auto/light/dark mode option. Comparing its stock `templates`/`blueprints` against the Mod Quark inventory above:
  - No stock equivalent at all (fully custom, as already assumed below): `icon-menu`, `feature-images`, `gallery-draggable`, `gallery-banners`, `lightbox`, `announcements`. (`quark2` does ship its own `modular/gallery.html.twig`, but per its README that only works with the premium Lightbox Gallery plugin, so it's no help here.)
  - Stock exists to re-diff against: `features.html.twig`, `base.html.twig`, `logo.html.twig`, `footer.html.twig`, `text.html.twig`.
  - **Possible scope reductions - check before porting:** `text.html.twig`'s `image_align` behavior may already ship natively in `quark2` (could drop our override entirely); `logo.html.twig` may not need a template fork at all - `quark2` documents `custom_logo`/`custom_logo_mobile` YAML options that might cover the 449 wordmark + wrenchman directly.
  - Optional, not required: `quark2` recommends the `github-markdown-alerts` plugin over the currently-installed `markdown-notices`. It also ships its own `quark2.php` (registers Twig helper functions), worth a look alongside `mod-quark.php`.

### Phase 1 - Stand up the parallel copy — done 2026-07-22
- [x] `sudo cp -a` live to a parallel path, loopback-only test vhost (SSH tunnel only, never public) - same as the 2.0 core migration. Copy at `/srv/robot-grav-site-quark2`; vhost `grav-quark2-test` on `127.0.0.1:8081` (same security rules as the live vhost, re-rooted, SSL/certbot stripped, reuses the existing `php8.3-fpm` pool). **Gotcha:** `sudo cp -a` copies `backup/` too (no exclude flag) - pulled in all 14 rotating backups (4.9 GB) for no reason. Brad removed it and `chown -R brad:editor`'d the whole copy so the rest of Phase 1 (and Phase 2) can proceed over plain SSH without a sudo prompt per step - this is a disposable test copy, not production, so that's fine; ownership goes back to `grav:editor` before any admin2-panel testing in Phase 3. **Second gotcha, briefly self-inflicted:** removing `backup/` trips Grav's Problems plugin, which treats it as a required folder and blocks the whole site with a diagnostic page instead of rendering - re-created it empty (`mkdir backup`) and it cleared immediately after a cache clear.

**Recurred 2026-08-01, with a twist:** `backup/` was missing again (whole site 500ing with the Problems page) at the start of a session - and by then ownership had been flipped to `grav:editor` per the note above, plus the top-level `/srv/robot-grav-site-quark2` directory itself carries a **POSIX ACL** (`getfacl` shows `group::r-x`, plus a named `user:yash:rwx` entry) that's *more restrictive* than the `ls -l` output suggests - `ls -l` displays the ACL **mask** in the group-permission column when an ACL is present, not the real `group::` entry, so it visually looked group-writable (`rws`) but wasn't. `brad` (in the `editor` group, not the named ACL user) couldn't `mkdir` or even `touch` a new file directly in that top-level directory as a result - subdirectories that already existed (`cache/`, `css/`, etc.) were unaffected, this ACL is specific to the top-level dir only. `sudo -u grav mkdir backup` (run by Brad interactively - `sudo` needs a real TTY, so `ssh -t` or running it from an already-open shell on the box, not a bare `ssh host "sudo ..."` from a script) fixed it. If this recurs, that's the fix; don't spend time on `chown`/`chmod` on the parent dir first, check `getfacl` before trusting `ls -l`.
- [x] Install Quark 2 via GPM on the copy only (`bin/gpm install quark2:1.1.6 -y -n` - separate package from `quark`, installs to `user/themes/quark2/`; confirmed not present anywhere yet, including live). Pinned to 1.1.6 as planned.
- [x] New child theme `user/themes/mod-quark-2/` inheriting Quark 2, same inheritance pattern Mod Quark uses today for Quark 1. Confirmed the actual mechanism (not obvious from the PHP class alone): `mod-quark.php` is just `class ModQuark extends Quark`, but the real template/asset fallback comes from a `streams:` block in `mod-quark.yaml` that lists both `user://themes/mod-quark` and `user://themes/quark` as `theme://` prefixes - Grav config does **not** cascade automatically between parent and child, only the stream lookup does. `mod-quark-2.php`/`mod-quark-2.yaml`/`blueprints.yaml` mirror this exactly, prefixes swapped to `mod-quark-2`/`quark2`, config defaults copied from `quark2.yaml` (accent-color `#8428DF`, theme-mode `auto`, etc.) the same way `mod-quark.yaml` duplicates `quark.yaml`'s.
- [x] Point the copy's `pages.theme` at `mod-quark-2` and confirm it renders as stock Quark 2 before porting anything. Verified 3x consistent across `/`, `/about-us`, `/about-us/history`, `/community`, `/join-us`, `/bunnybots`, `/admin` (all 200) and `/api/v1/sync` (403) - real Quark 2 fingerprints present (`data-theme-default`, `q2-accent`, Blades CSS classes), actual 449 nav/content/logo rendering through it, `grav.log` clean. Brad confirmed visually via SSH tunnel (`ssh -L 8081:127.0.0.1:8081 449-live`, browsed `127.0.0.1:8081`). **Gotcha for later phases (including the real Phase 4 cutover verification):** PHP's opcache only revalidates file changes every ~2s (`opcache.revalidate_freq`), and different FPM workers pick up an edit at slightly different times - a page can 500 right after a file change and then pass cleanly a few retries later with no further changes made. Don't treat one immediate post-edit failure as conclusive; retry after a few seconds before debugging further.

### Phase 2 - Port each piece, one at a time — done 2026-07-22
- [x] `icon-menu` - FA7 icons verified against the exact CDN bundle Quark 2 loads (7.0.1), not guessed from memory; several real renames needed (`fa-money`→`fa-money-bill`, `fa-pencil-square-o`→`fa-pen-to-square`, `fa-android` needs `fa-brands` not `fa-solid`). **Self-caught regression:** first pass dropped the `text_align` style hook and switched the click-target to wrap the whole card instead of just icon+header - both restored to match current behavior exactly after Brad flagged the general principle (see "Open decision" discussion 2026-07-22: this migration should not bundle design changes, full stop, team decides those separately later).
- [x] `feature-images` - ported verbatim (image resolution, thumbnail sizing, `-original` full-size linking, `separate_links` mode are all Grav-core/theme-agnostic, no adaptation needed beyond the same grid-CSS fix as icon-menu).
- [x] `gallery-draggable` / `gallery-banners` - ported verbatim, byte-for-byte; both use fully custom BEM classes with zero Spectre/Blades framework dependency.
- [x] `footer-col` / footer partial - kept the custom version as-is rather than switching to `quark2`'s stock footer, per the same no-bundled-redesign principle; stock option remains available for the team to reconsider later.
- [x] `announcements` partial + `announcements-data` - ported verbatim. `sponsors-data` confirmed absent from live, staging-only pending the business-team decision (unrelated to this migration).
- [x] `logo.html.twig` - kept the `<img>`-tag version rather than switching to `quark2`'s inlined-SVG `custom_logo` mechanism; the inlined approach would have activated a then-dormant `header-dark` CSS rule that doesn't apply under our setup anyway, but the plain version is the more conservative, unambiguously-identical choice regardless.
- [x] `error.html.twig` - ported the actual custom 404 page (`user/pages/11.error/`, a real photo + "Team 404" joke) - **not** the generic error-plugin default I initially checked, which made the `#error img`/`h1` CSS look like dead code when it very much isn't.
- [x] `base.html.twig` - used Quark 2's own modern structure (semantic header/nav, sticky/animated JS, theme-toggle, mobile overlay) rather than porting the old Spectre-era markup, since the old markup would fight Quark 2's own JS. Carried the 449-specific content into it: GA4 snippet, full Open Graph/Twitter/JSON-LD SEO block, the `announcements` include, the `underconstruction` banner check. Dropped two dead JS references (`site.js`/`jquery.treemenu.js` under `mod-quark/js/` don't exist as files - they were resolving to stock `quark`'s own `site.js` via the stream cascade the whole time) and unused `line-awesome.min.css`.
- [x] `features.html.twig` (unused by any current page, ported for completeness), `macros.html.twig`, `radio.html.twig`. **Near-miss caught before shipping:** `macros.html.twig` had never actually been overridden yet at the point I checked it - it was silently falling back to `quark2`'s stock nav macro the whole session, which is what had been drawing the dropdown chevrons (▾) in every screenshot so far. A verbatim port of Mod Quark's version would have deleted the `has-children` CSS class those chevrons depend on. Merged instead: kept Quark 2's chevron structure + its unused-but-harmless `menu_icon` option, added in Mod Quark's `notclickable` (confirmed live on the "Sponsors" nav item, `05.sponsor-information/modular.md`).
- [x] `text.html.twig` - the Phase 0 hunch that `quark2`'s native `image_align` might make this droppable was **wrong**; the real template does far more (Team History year-summary card parsing via `<blockquote>`/`<h3>` splitting, `year_bar` support, its own alt-text fallback, full-size image linking) that Quark 2 has no equivalent for at all. Ported in full. Confirmed the existing `image_align` left/right toggle is itself nearly cosmetic on the current site (no matching CSS at all, just a 30px padding difference) - preserved that as-is rather than "fixing" it into a real toggle.
- [x] Full `custom.css` audit, rule by rule - see the three gotchas above (Blades `.columns`, missing `box-sizing:border-box`, Quark1-vs-Quark2 scroll mechanism). Biggest find: an entire block of Team History CSS (the blockquote-as-table-row styling and the actual `order:1`/`order:2` flex mechanism that puts images on one side and text on the other for year-modules specifically) had been missed during the `text.html.twig` port and wasn't caught until this dedicated audit pass. `custom.css` is now 508 lines (was 479) - net new lines are added context comments, not new rules; every original rule has a home.
- [x] Carried the disabled BlairMdITC `@font-face` block forward verbatim, including the explanatory comment and the commented-out `h1` font-family line (only path reference updated: `mod-quark/fonts/` → `mod-quark-2/fonts/`).
- [x] Cache-bust mechanism confirmed **and fixed** - it didn't exist at all on the child theme's `custom.css` until this phase (see the gotcha note above); now versioned `?v=N`, bump on every edit.
- [x] **Decided (Brad, 2026-07-22): accent-color `#a80007` (449 red), pinned to light-only.** `theme-mode: light` in `mod-quark-2.yaml`, both `theme-toggle.html.twig` includes removed from `base.html.twig` (header + mobile menu - no toggle button renders), pre-paint bootstrap script simplified to force `data-theme="light"` unconditionally rather than checking `localStorage`/OS preference. `theme-toggle.js` is still loaded (harmless, nothing for it to attach to) - not worth touching the asset pipeline further for this. Quark 2's dark-mode CSS is otherwise fully intact if the team wants to revisit this later.

### Phase 3 - Verify (still not public)
- [x] `curl` sweep of every page, not just the homepage. All **70 routable pages** (pulled from live's own `sitemap.xml`) return `200` on the test copy.
- [x] Eyeball image-bearing pages, not just status codes (bit us once already during the 2.0 core migration). Checked every `<img src>` (not just the page shell) on the 15 most image-dense pages by content, ranked by raw `<img>` count on the test copy: `/about-us/history` (66), `/about-us/history/robots` (37), `/` (36), `/about-us/mentors` (28), `/about-us/history/449-members-of-note` (26), `/sponsor-information/current-sponsors` (18), `/about-us/history/t-shirt-designs` (17), plus 8 more. **301 images checked, 0 real breakage from the migration.** 13 flagged initially: 12 were false positives from the check script itself not URL-encoding spaces/parens in filenames (`_MG_7593 (2) (1).jpg` etc. - confirmed fine once properly encoded); the 1 real 404 (`blank.png` on `/about-us/mentors`) is a **pre-existing content typo, identical on live** - the page's own `feature-images.md` frontmatter references `blank.png` but the actual uploaded file is `blank.jpg` (confirmed via `media_order` in the same file) - a one-character-extension mismatch in content, unrelated to the theme port, not fixed here.
- [x] Admin2 pass: sign in, edit one page of each custom module type, confirm Save and content-seed still work. **4 of 5 clean** (`icon-menu`, `feature-images`, `gallery-draggable`, `text`/`hero` all edit+save+revert cleanly). `gallery-banners`' drag-to-reorder (the Banners-tab list field) doesn't work - but confirmed **also broken on live**, unmodified, so it's a pre-existing admin2 bug, not a migration regression. Out of scope here.
- [x] Re-test gallery drag-reorder. Confirmed working (native admin2 Page Media "Reorder" control on `gallery-draggable`, per the retirement note above).
- [x] `grav.log` clean - no new errors from today's edits. The log **does** contain a handful of `grav.CRITICAL` "syntax error... expecting ']'" entries, but they're all byte-identical to entries in **live's own** `grav.log` at the same timestamps (2026-07-15, 07-18, and 07-22T13:38:28 - all predating or coinciding with the Phase 1 `cp -a`, which copies `logs/` along with everything else). Inherited historical log content, not anything new. Separately worth knowing: this looks like a real, recurring, low-grade **live** issue (a compiled language-cache file occasionally getting corrupted/truncated - garbled fragments like `PAG`/`се`/`REN` mid-file, classic concurrent-write corruption) - self-heals on next request, unrelated to Quark 2, not investigated further here.
- [x] Screenshot comparison vs current live, desktop - **2 real regressions found and fixed**, 1 item deferred:
  1. **Fixed: `header-dark` missing from the `body_class()` whitelist in `mod-quark-2/templates/partials/base.html.twig` line 1.** Live's array has 5 entries (`header-fixed, header-animated, header-dark, header-transparent, sticky-footer`); the port had only 4. Restored to match.
  2. **Fixed: `#header` used Quark 2's native `position: sticky` instead of Mod Quark's `position: fixed`**, breaking the "hero renders full-bleed behind the transparent header" effect (this exact fixed-header/hero interaction is already flagged as fragile on this site - see the Banners-feature lessons-learned entry on manual top-offsets). Sticky reserves its own space in flow, pushing the hero down instead of letting it start at y=0 behind the header. Fix - scoped override in `mod-quark-2/css/custom.css` (bumped `?v=11` → `?v=12`):
     ```css
     body.header-transparent #header {
       position: fixed;
       left: 0;
       right: 0;
     }
     ```
     Scoped to `.header-transparent` only, so every normal (non-hero) page keeps Quark 2's sticky default untouched. Verified both states match live exactly: transparent/white-text at page load (hero photo visible through the header), solid 449-red with compact nav (Quark 2's own `.scrolled` min-height rule) once scrolled.
  3. **Fixed (2026-07-23): nav + hero base typography reconciled.** Phase 2's CSS audit covered custom components/colors, not Quark 2's own native nav/hero rules. Measured side-by-side at a 1175px viewport (computed styles, live vs test copy), then fixed each property, appended to `mod-quark-2/css/custom.css` (bumped `?v=12` → `?v=13`):

     | Property | Live (Spectre) | Test copy before | Test copy after fix |
     |---|---|---|---|
     | Nav link font-size | 13.6px | 15px | **13.6px** |
     | Nav link font-weight | 700 | 500 | **700** |
     | Nav link spacing | padding `7px 30px 7px 20px` (50px combined) | `gap:20px`(*) + padding `8px 15px` (30px combined) | **padding `7px 30px 7px 20px`, gap zeroed - matches live's mechanism exactly** |
     | Hero content position | flex `column`, `align-items:center`, `justify-content:center` inside a `min-height:100dvh` box - H1 @ 305px from top (1175×660 viewport) | no flex/centering at all on `.hero`, just `padding-block` - H1 @ 141px from top | **flex-centered to match live's actual mechanism - H1 @ 287px from top (18px residual, from live's H1 having its own 38.95px margin-top that Quark 2's h1 rule doesn't set; visually negligible, not chased further)** |

     (*) the 20px gap I originally measured on `.desktop-menu` was a red herring - that element has a single child so its `gap` has no visible effect; the real inter-link spacing came from `.dropmenu ul.navigation`'s `gap: 0.5rem` (8px), now zeroed to match live's padding-only mechanism.

     Root-font-size (20px live vs 16px Quark 2 default) was never the real driver - fixed properties in absolute `px`, not `rem`, sidesteps it entirely. Deliberately fixed the **mechanism** (flex-centering) rather than tuning a magic padding number for the hero, since a magic number would only be correct at one specific viewport/content-height combination.

     Checked and confirmed **not actually different** (screenshot-scale illusion in an earlier comparison, not a real gap): announcement-pill padding (10.1×21.8px vs 10.4×22.4px) and H1 font-size (50px both) - untouched.
- [x] Screenshot comparison vs current live, mobile (375px viewport). Found and **fixed** (same session, same root cause as item 3):
  4. **Fixed: homepage announcement banner overlapped the H1 hero title on mobile**, because the hero's un-centered content sat high enough to collide with the banner's `position:absolute; top:4.75rem`. The hero flex-centering fix in item 3 resolves this directly - verified via a fresh mobile screenshot, banner and title now sit cleanly stacked with normal spacing, matching live. Only ever reproduced where a `.hero` + the announcement banner co-occur (homepage); checked `/about-us/history/robots` and `/about-us/mentors` too, both clean both before and after (no banner shown there).
- [x] **Fifth regression found 2026-07-23, via Brad's own side-by-side screenshots** (not caught by any of the checks above - genuinely needed a human looking at both sites at once): icon-menu and Sponsors modules rendered with narrow, badly-wrapped columns and big gaps between them on the test copy, instead of filling the row like live. **Root cause: a silent CSS parse bug from Phase 2, not a Phase 3 regression.** The comment directly above the `.modular-features .columns` grid-fix rule in `custom.css` contained the literal text `col-*/col-md-*/col-sm-*` - and that `*/` sequence **prematurely closed the CSS comment** mid-sentence. Everything from there to the *real* closing `*/` got swallowed into one giant invalid selector, silently dropping the entire rule (including `display: flex`) - so Quark 2's own stock `.modular-features .columns { display: grid; ... }` won by default with nothing to override it. This has been broken since Phase 2 and wasn't caught by any curl/computed-style check because the page never errored - it just silently used the wrong (but valid-looking) layout. Confirmed via the browser's own parsed `document.styleSheets` (the rule was completely absent from the parsed stylesheet, not just losing a specificity fight) before concluding this, not guessed at.

     Fix: reworded the comment to remove the embedded `*/` (`col-*, col-md-*, col-sm-*` - commas instead of slashes), bumped `custom.css?v=13` → `?v=14`. Verified via computed styles: `.columns` now correctly reports `display: flex`, and columns are exact 33.3%/25% widths of their container (icon-menu and Sponsors respectively), matching live's math. Also re-checked the whole file's `/*`/`*/` count (25/25, balanced) to confirm no other instance of this same mistake is lurking elsewhere.
- [x] **Sixth and seventh regressions, found the same way immediately after** (Brad: "Sponsors module is a bit better, but still divergent - eg the visible border around each tile", plus the footer appearing blank in his screenshot):
  6. **Fixed: icon-menu/Sponsors columns still had Quark 2's own card styling** (`box-shadow`, white `background`, `1.5rem 1.25rem` padding) even after fix #5 restored the right grid mechanism - Phase 2's audit fixed the *layout* (flex vs grid) but never stripped Quark 2's *card look* from the same `.modular-features .column` rule, since that gap wasn't visible until the columns were actually laying out correctly side by side. Added `box-shadow: none; background: transparent;` and replaced the padding-left/right-only reset with a full `padding: 0 0.4rem` (zeroing the vertical padding Quark 2's shorthand still had). Bumped `?v=14` → `?v=15`.
  7. **Fixed: footer text rendered dark gray instead of white** (looked "blank" against the dark red footer background in a screenshot, though the content and background color were both actually fine). Root cause: Blades' base stylesheet sets `td, th { color: var(--pico-color); }` directly on every table cell - and a directly-set property on an element always wins over an inherited value from an ancestor, regardless of specificity math, so this beat the white color the footer text was relying on via plain inheritance from `footer.site-footer { color: #fff }`. Fix: added `color: #fff` directly to the existing `.footer-cell` rule (same `?v=15` bump). `.footer-title-cell` already had its own explicit `color: #fff` and was never affected.

     Both confirmed via computed styles after the fix (box-shadow: none, transparent background, white footer text) - not just re-reading the CSS source, since #5 already proved that isn't sufficient evidence on its own.

- [x] **Built a real automated comparison tool** (`~449-quark2-compare/compare.js` on the Air, `blotchy-macbook` / `100.117.250.37` - separate from `headline-lab`, which just supplies its already-installed Playwright via `NODE_PATH`). Loads a curated list of 18 pages on both live and the test copy, at desktop (1175×900) and mobile (375×812), takes full-page screenshots, and diffs computed styles for a checklist of signature selectors (nav link, h1, footer cell, announcement pill, icon-menu/Sponsors column). Reaches the test copy over Tailscale: the existing `ssh -L 8081:127.0.0.1:8081 449-live` tunnel got a second, additive bind to the MBP's own Tailscale IP (`ssh -L 100.119.94.34:8081:127.0.0.1:8081 449-live`, run alongside the original, not replacing it), which the Air can reach directly. No new credentials touch the droplet - same single `claude_449` key as always. Run it with:
  ```bash
  ssh brad-developer@100.117.250.37 "cd ~/449-quark2-compare && NODE_PATH=/Users/brad-developer/headline-lab/node_modules /opt/homebrew/bin/node compare.js"
  ```
  Output: `report.json` (every selector/page/viewport that differs, with both values side by side) and `screenshots/` (full-page PNGs, `live_*`/`q2_*` prefixed). This is what actually found and verified everything below - eyeballing screenshots alone had already missed all of it once.

  **Currently broken 2026-08-01:** Playwright failed with `ENOSPC: no space left on device` - the Air's disk shows only ~103Mi available out of 228GB (`df -h /`). Not caused by this tool (its own temp footprint is a few MB); the bulk of what's in `/var/folders/.../T/` is unrelated OneDrive updater temp files (~800MB) and the rest is unaccounted for by any single user-visible directory, so it's most likely local Time Machine snapshots or something else system-level holding space - Brad is investigating. Don't assume this tool works without checking `df -h /` on the Air first. Fell back to `mcp__claude-in-chrome__*` (the user's real Chrome) for direct screenshot/computed-style verification instead - see the tool note further down; slower (manual, one page/viewport at a time) but works.

- [x] **Eighth through twelfth regressions, found by running the tool across every page/viewport instead of just the homepage at one width:**
  8. **Fixed: active nav link (current page) was font-weight 600 instead of 700.** Quark 2's own `.dropmenu a.active` sets a lighter weight for the current page's nav item; live's `.active` handling only ever changes color, never weight. Invisible on the homepage (nothing's "active" there) - only showed up once the tool checked pages where a nav item highlights. Added `.dropmenu a.active { font-weight: 700; }`.
  9. **Discovered live's root font-size is fluid, not a fixed 20px** - `calc(16px + 4 * ((100vw - 480px)/800))` between 480px and 1280px viewports (Spectre), floored at 16px, ceilinged at 20px. Everything measured earlier (nav font-size, the announcement pill) was only checked at one desktop width, so the earlier hardcoded-px fixes were only correct at that width - confirmed by the tool: live's nav font-size measured **11.2px on mobile vs 13.6px on desktop**, while the Quark 2 fix stayed fixed at 13.6px on both.
  10. **Fixed: nav-link font-size and announcement-pill font-size now use `clamp()` to replicate live's exact fluid formula**, verified against both the 375px and 1175px data points (not just theory) - nav: `clamp(11.2px, 9.52px + 0.35vw, 14px)`; pill: `clamp(12.8px, 10.88px + 0.4vw, 16px)` (added explicitly - `.site-announcement` never had its own font-size before, only inherited).
  11. **Fixed: icon-menu/Sponsors column padding was a fixed `20px`, but live's is also `1rem` of the same fluid root** - found via the tool showing `19.475px` (live, desktop) vs `20px` (Quark 2) and `16px` (live, mobile) vs `20px` (Quark 2, unchanged). Same `clamp(16px, 13.6px + 0.5vw, 20px)` treatment - this padding comes from **Quark 1's own stock `theme.css`** (`.modular-features .column { padding: 1rem }`), not from Mod Quark's custom.css at all, so Phase 2's audit never had a reason to look for it.
  12. **Fixed: icon-menu/Sponsors columns were ~14-23% too wide.** Live wraps these modules in `<section class="container grid-lg">` (Spectre, `max-width:976px`); the ported markup uses plain `<div class="container">` (Blades, `max-width:1200px`), so nothing constrained it at most viewport widths. Also found Blades' own `.container` padding-inline (`clamp(1rem,4vw,2rem)`, up to 32px) is far more generous than Spectre's `0.4rem` (~7.79px), and that live's outer `<section class="modular-features...">` wrapper itself carries a **universal 1rem `.section` padding from Quark 1's stock theme** that Quark 2 has no equivalent of at all (so every module sat flush to the viewport edge on mobile). Added, all scoped and fluid: `.modular-features > .container { max-width: 976px; padding: clamp(6.4px, 5.44px + 0.2vw, 8px) }` and a **general, non-scoped** `.section { padding: clamp(16px, 13.6px + 0.5vw, 20px) }` (this one isn't specific to icon-menu/Sponsors - it likely affects other modules too, not otherwise audited here). Bumped through `?v=16` → `?v=19` across these five fixes.

     **Result, re-verified with the tool across all 18 pages × 2 viewports:** nav-link and announcement-pill now match live exactly everywhere. Icon-menu/Sponsors column width is within <1px of live at desktop (sub-pixel, effectively pixel-perfect) and exact at mobile. Only remaining diff anywhere is the already-known, already-accepted H1 vertical-position residual (varies by page content, ~15-40px, from live's H1 having its own margin-top that Quark 2's h1 rule doesn't set).

     **This "fully converged" claim was premature** - see the next entry. The computed-style checks above were real and correct as far as they went, but they only ever checked *individual* element properties (one column's own width/shadow/padding), never whether elements actually **fit together** (do all 3 columns end up on the same row) or whether *other, unrelated* stock-theme properties (icon size, heading line-height) were silently still wrong. Brad kept reporting "still divergent" after each "fixed" round, correctly, and pushed back on accepting computed-style output over what the page actually rendered - that pressure is what surfaced items 13-15 below.

- [x] **Thirteenth through fifteenth regressions, found 2026-07-23 by taking Brad's repeated "still divergent" reports at face value instead of re-trusting the same narrow checks:**
  13. **Fixed: icon-menu/Sponsors STILL actually wrapped (2 columns + 1 orphaned onto its own row), despite every individual column measuring the correct width.** Root cause: Quark 2's own stock `.modular-features .columns` rule sets `gap: 1.25rem` (20px) as part of its native grid layout - the item-5 fix overrode `display` to `flex` but never touched `gap`, so it silently carried over. Two 20px gaps across 3 columns = 40px more than the row had room for even though each column's own width was correct, forcing `flex-wrap: wrap` (also still on) to kick in. **This is exactly the blind spot named above** - checking one column's `getBoundingClientRect()` never reveals that its sibling wrapped to a new line. Fixed with `gap: 0` on `.modular-features .columns`; found and fixed the identical latent bug on `.modular-text .columns` too (same pattern, bigger gap - `3.5rem` from Quark 2's own responsive text-grid rule). Confirmed this time by checking **all three columns' `top` position**, not just one column's own properties - all three now share the same `top`, genuinely one row. Bumped `?v=19` → `?v=20`.
  14. **Fixed: icon-menu icons were rendering at browser-default 16px instead of live's 130px.** Yet another "lives in Quark 1's stock `theme.css`, never in Mod Quark's own custom.css, so Phase 2 never had a reason to check it" gap (fourth instance of this exact pattern this session) - stock theme sets `.feature-icon { font-size: 130px; height: 100px; margin: 1rem 0; display:flex; align-items:center; justify-content:center; }` and `.feature-icon i { font-size: 70px; position:absolute; top/left:50%; transform: translate(-50%,-50%); }`, replicated as fluid `clamp()` (ratios 6.5rem/5rem/3.5rem respectively, same fluid-root technique as everything else). Bumped to `?v=21`.
  15. **Fixed: feature heading (`h6`) line-height was 21.6px instead of live's `1` (18px)** - same pattern again, stock theme sets `line-height:1` on `.feature-icon h6`, added explicitly. Bumped to `?v=22`.

     After 13-15, per-column height converged much closer (icon-menu column: was off by 89px, now off by ~26px) but **the icon-menu/Sponsors *section* height still doesn't fully match live** (icon-menu section ~150px taller, Sponsors ~106px taller, confirmed via a dedicated Playwright section-height-comparison script, not just eyeballing) - timing-race-tested too (measured immediately on load and again 3s later, identical both times, so it's a real layout fact, not a YouTube-iframe-sizing race). Not yet root-caused.

**Systematic pattern identified 2026-07-23, audit actually run 2026-08-01 - this is the actual state of things, not "Phase 3 done":** every regression in 11, 12, 13, 14, and 15 above turned out to be the *same underlying gap*, just in a different property each time: **Quark 1's stock `user/themes/quark/css-compiled/theme.min.css` (and to a lesser extent `spectre.min.css`) provides real, load-bearing CSS for classes that Mod Quark's *own* `custom.css` uses but never restates** (because the child theme gets it "for free" via inheritance from the parent). Phase 2's audit only ever diffed Mod Quark's custom.css against itself / against what Quark 2 needed - it had no way to catch a property that live gets from a file the audit never looked at. Finding these one CSS property at a time (as items 11-15 did) clearly wasn't converging fast enough, so a real tool was built for it.

**The task, precisely:** for every CSS selector used in live's `user/themes/mod-quark/css/custom.css`, check whether live's `user/themes/quark/css-compiled/theme.min.css` (stock Quark 1) *also* defines rules for that same selector - and if so, whether `mod-quark-2/css/custom.css` (or Quark 2's own `user/themes/quark2/css/theme.css`) currently provides an equivalent. Anywhere it doesn't, that's a gap of the exact same shape as items 11/14/15. **Built and run 2026-08-01:** a Python + `tinycss2` script (selector → property dict per file, `@media` blocks flattened/recursed, comma-separated selector groups split on top-level commas only) comparing the six files below, fetched via plain `curl` (no auth needed, all public over HTTPS/the tunnel). Script + raw `report.json` are session-scratch, not committed to this repo - rerun is cheap (~1 min) if needed, the method matters more than the artifact:
  - `https://robot.mbhs.edu/user/themes/mod-quark/css/custom.css` (live's custom overrides - the selector list to check)
  - `https://robot.mbhs.edu/user/themes/quark/css-compiled/theme.min.css` (Quark 1 stock - where the hidden gaps live)
  - `https://robot.mbhs.edu/user/themes/quark/css-compiled/spectre.min.css` (Spectre framework - lower priority, base-level classes)
  - `http://127.0.0.1:8081/user/themes/mod-quark-2/css/custom.css` (test copy's current state - what's already fixed)
  - `http://127.0.0.1:8081/user/themes/quark2/css/theme.css` (Quark 2 stock - may already cover some gaps natively)
  - `http://127.0.0.1:8081/user/themes/quark2/css/blades.min.css` (Blades/Pico base - lower priority)

  **Result: 109 selectors in live's custom.css checked. Tier 1 (vs Quark 1 stock theme.min.css) flagged 12 selectors with missing properties; Tier 2 (vs Spectre, lower priority) flagged 10.** Triaged by hand against actual rule bodies (not just the flagged property list) - most Tier 1 hits were **false positives from exact-selector-string matching**, not real gaps:
  - `.header-dark #header:not(.scrolled)` family (5 selectors): Q2's custom.css already covers these, just restructured as `body.header-dark:not(.scrolled) #header` to match Quark 2's own scroll-class mechanism (`.scrolled` toggles on `<body>`, not `#header`, in Quark 2's JS) - confirmed by reading both rule bodies directly, values match exactly.
  - `.hero h1` font-size: flagged as 2rem missing, but this is a media-query-dependent value my flattening approach can't resolve correctly (theme.min.css has 3 breakpoint values for this selector - 2rem/3rem/4rem - and the script just took last-in-file-order). Phase 3 item 3 already verified this matches live in a real browser at 1175px (50px both) - trust that over the static re-check.
  - `.hero h1` color (#242931): the stock **base** rule, but live's actual homepage hero carries a `text-light` class, activating `.hero.text-light h1{color:#fff}` instead - confirmed via live's homepage HTML. Quark 2's own theme.css already has the equivalent `.hero.text-light h1{color:#ffffff}` rule. Not a gap for any page using text-light/text-dark (which appears to be all of them so far) - low-priority edge case only if some page uses neither class.
  - `#error`, `.modular-features .frame-box`: not yet checked for real-world impact (error page styling, and `.frame-box` template usage unconfirmed) - deferred, low suspected impact.
  - `#header` font-size/font-weight/width/height: **still an open, unresolved, plausibly-real gap** - live's own custom.css `#header{}` rule only sets background/color (confirmed by reading it directly), so the sizing comes purely from Quark 1 stock. Quark 2's stock `#header` uses a different mechanism entirely (`#header .navbar { min-height: 72px }` vs live's `#header { height: 4rem }` = 64px directly) - not restated anywhere in Q2's stack. Plausible contributor to the still-unexplained section-height residual from item 15's note, but not yet visually verified either way. **Next session should check this one.**
  - Tier 2 (Spectre) items not yet triaged at all - `.button`/`.button:focus`/`.button:hover` (full button styling, potentially significant if buttons are used on any page), `body`/`p`/`h1`/`blockquote` base typography, `.float-left`/`.float-right`. **Next session should triage these.**

  16. **Fixed and verified 2026-08-01: `.modular-features .feature-icon` / `.feature-icon i` had completely wrong colors** (icons rendered in 449 red / near-black instead of live's pale gray `#e7e9ed` / `#acb3c2`), and **`.modular-features .feature-icon h6` was missing `background:#fff`, `color:#667189`, and `text-transform:uppercase`**. Important: **this gap was invisible to the systematic audit above** - `.feature-icon` and `.feature-icon i` (bare, unprefixed) are never referenced in live's `custom.css` at all, so they were outside the audit's own selector scope by construction (the audit only checks selectors that appear in custom.css). Found instead by direct side-by-side screenshot comparison (Claude in Chrome, not the in-app Browser pane - see tool note below), which is exactly the kind of check Brad kept asking for. Fix: added `color` to the existing sizing rules from item 14 (`.modular-features .feature-icon` / `.feature-icon i`, both flat hex values ported as-is, no clamp needed since color isn't viewport-dependent), and added the three missing properties to the existing `.modular-features .feature-icon h6` rule. Bumped `custom.css?v=22` → `?v=23`. Verified two ways: computed styles after the fix are byte-exact matches to Quark 1's stock values (`rgb(231,233,237)`, `rgb(172,179,194)`, `rgb(255,255,255)`, `rgb(102,113,137)`, `uppercase`), and a fresh screenshot shows pale-gray icons with a blue-gray uppercase heading matching live, all three columns still sharing one row (no layout regression from the change, as expected since color doesn't affect box model).

**Tool note for future sessions - screenshot verification:** the in-app Browser pane (`mcp__Claude_Browser__*`) has a real bug where a screenshot taken shortly after a JS-driven scroll (`window.scrollTo`/`scrollIntoView`) can come back **completely blank white**, even though the DOM/computed-styles/`elementFromPoint` all confirm correct content is there - reproduced identically on live and the test copy both, so it's a tool quirk, not a page bug. **Claude in Chrome** (`mcp__claude-in-chrome__*`, the user's real Chrome via extension) does not have this problem and is more reliable for this project's verification needs - use it instead when available. Separately: live's homepage hero has a `parallax` class that appears to swallow synthetic mouse-wheel/keyboard scroll input (native `computer` `scroll`/`Page_Down` silently no-ops there, though `window.scrollBy`/`scrollTo` via JS works fine and Claude in Chrome renders correctly after it) - the test copy's homepage doesn't have this problem. Also: a second, background (non-frontmost) Chrome tab returned blank screenshots repeatedly - stick to one tab, reusing it via `navigate` between live/test-copy URLs, rather than opening a second tab for side-by-side comparison.

### Phase 3a - Re-sync to live (2026-09-08) - the target moved while the migration sat idle

**Nothing happened on this migration between 2026-08-01 and 2026-09-08, but live kept being developed - so the test copy was five weeks stale against a moving target.** Anyone picking this up should check for drift *first*; the pixel-calibration items below were all measured against a live site that has since changed. Drift found and re-synced this session:

| What | Live | Test copy before re-sync |
|---|---|---|
| `custom.css` | `?v=67`, modified 2026-09-04 | `?v=23`, modified 2026-08-01 |
| Body / h2-h6 font | **Inter** (Google Fonts) | system stack |
| `h1` font | **Science Gothic** + `-webkit-text-stroke` + 2-layer `text-shadow` | system stack, no stroke/shadow |
| Font loading | 2 `preconnect` + Google Fonts `<link>` in `base.html.twig` | absent |
| `gallery-press` module | added 2026-08-13 (template + blueprint + 12 CSS selectors) | **not ported** |
| `sponsorship-levels.css` | added 2026-07-23, loaded by `text.html.twig` | **missing entirely** |
| `text.html.twig` image loop | rewritten Jul 30 - Aug 3 | stale (pre-rewrite) |
| `gallery-banners` | real `.resize(null,400)` + alt-text blueprint field | stale `.height(400)` |
| `press-coverage` / `.whats-new` CSS | present | absent |

**What was ported, and how each was verified.** Bumped `custom.css?v=23` → `?v=25`.

  17. **Typography (the Sept 4 deploy).** Ported as a *mapping*, not a copy, because the two themes split fonts differently: `quark2/css/theme.css` applies `--pico-font-family-display` to `h2,h3,h4,h5,h6,.display,.h-display` and `--pico-font-family-sans-serif` to `body`, while **`h1` is in neither list** - it has its own rule that sets no `font-family` at all and just inherits body's. So both Pico variables go to Inter (covering body + h2-h6, exactly what live wants) and h1's Science Gothic is set in the h1 rule, same as live does it. The old font block in `custom.css` wasn't merely incomplete, it was **actively asserting something false** ("current site never overrides Spectre's default font-family") - rewritten rather than appended to. Also added `button, input, select, textarea { font-family: inherit }` (form elements ignore inheritance per UA stylesheet) and the Google Fonts `<link>` + preconnects in `base.html.twig` (a `<link>`, not an `@import` - Grav's concatenated CSS pipeline silently drops a mid-file `@import`). **Verified:** computed styles on the test copy now byte-match live (`body: Inter, sans-serif`; `h1: "Science Gothic", sans-serif` / 50px / `1.5px rgba(0,0,0,0.55)` / matching two-layer shadow / uppercase; `h2: Inter, sans-serif`). One trap worth knowing: `document.fonts.check('16px Inter')` returns **false** on the test copy but **true** on live - a red herring, because Quark 2 additionally registers Cal Sans and Font Awesome 7 faces, which confuses face-matching. Proved Inter genuinely renders by canvas text measurement instead (Inter 317.8px vs bogus-font fallback 277.7px vs generic sans-serif 303.3px - three distinct widths).
  18. **`gallery-press` module ported** (template + blueprint copied verbatim - it uses its own BEM classes with no Spectre/Blades dependency - plus its 12 CSS selectors). **Verified in a way worth repeating:** the module renders nothing on *live* because live's `gallery-press.md` is `published: false` (an unpublished draft), so "both sides render nothing" was true but proved nothing. Temporarily set `published: true` **on the test copy only**, confirmed a correct 4-across grid (`275px` columns, `28px 20px` gap, four items sharing `top:29` and two sharing `top:379` - genuinely one grid, siblings fitting together, not just individually-correct boxes), captions/credits/`Download original — WxH` links all correct, then **reverted to `published: false`** to restore parity with live.
  19. **`text.html.twig` image loop re-ported** - names from `media_order` with a `page.media.images|keys` fallback, `{% if image %}` null check, `-hover.<ext>` pointer-swap support, `.resize(1200)` replacing the old `.height(...)` (which was never a real resize - it only set a cosmetic HTML attribute, so browsers downloaded full originals), and `image.filename`/`image.basename` corrections. The container adaptation (`<div class="container">` vs live's `<section class="container {{ grid_size }}">`) was **preserved**, not overwritten. Live's dead `_column_w` / `image_width` field is carried over verbatim with a comment rather than silently "improved" - it's a known open item on live (Kiran's to fix; the hardcoded 1200 vs the unused 600 config field). **Verified:** `<img>` counts on the test copy now match live exactly on `/about-us/history` (66=66), `/about-us/history/robots` (37=37) and `/about-us/mentors` (28=28), with **zero** `src=""` (the null check working). `/sponsor-information/current-sponsors` is 18 vs 17, traced to genuine **content** drift (live gained GHF and Dow STEM logos, test copy still has an older DoD STEM logo), not a template fault.
  20. **`gallery-banners` template + blueprint synced verbatim** (confirmed first that the ported copy had *no* Quark 2 adaptations, so verbatim was safe) - brings the real `.resize(null, 400)` and the per-banner alt-text field. **`sponsorship-levels.css`** copied and its `<link>` restored in `text.html.twig`. **`press-coverage` + `.whats-new .news-archive-link` CSS** appended.

  **Deliberately NOT ported: live's `body.no-hero #start { padding-top: 4rem }`.** That rule is a Quark 1-specific workaround - its own comment explains it compensates for Quark 1's `_framework.scss` `#body-wrapper` header-fixed padding never reaching a modular page. Quark 2's header is `position: sticky` and reserves its own flow space, so porting it verbatim would likely add 4rem of dead space at the top of every hero-less page. **Open item: verify against a hero-less page on both sites before deciding.**

  **How the port list was made systematic rather than "the things I happened to notice":** a selector-set diff of live's `custom.css` against the ported one (same tinycss2 approach as the 2026-08-01 audit; script is session-scratch, ~40 lines, cheap to rewrite). It reported 36 selectors present on live and absent from the port. Triage: 12 were gallery-press, 10 press-coverage, 1 `.whats-new`, 1 `no-hero` (deliberately skipped, above); **7 were the already-known `.header-dark #header:not(.scrolled)` false positives** (the port has them restructured as `body.header-dark:not(.scrolled) #header` to match Quark 2's body-level `.scrolled` class - same finding as the Aug 1 audit); and **`h2,h3,h4,h5,h6` was a false positive too**, satisfied via the `--pico-font-family-display` variable rather than a matching selector. Worth rerunning after any future live-side CSS work.

  **Verification sweep after all of the above:** 17 representative pages checked on the test copy - 15×200, plus `/community/stem-nights` 404 and `/sponsor-information` 301, **both of which match live exactly** (the former is a staging-only page, the latter a redirect). No Twig/PHP error text in any rendered page, and `grav.log` gained nothing new (its only CRITICAL entries remain the inherited 2026-07-22/23 ones documented above).

  21. **ROOT-CAUSED AND FIXED 2026-09-08: the icon-menu/Sponsors section-height residual, open and unexplained since 2026-07-23.** Re-baselined against the re-synced copy first, as the entry above required - and **the residual turned out not to be typography-related at all.** The numbers barely moved across the font swap (icon-menu +154px / Sponsors +98px measured now, against ~150px / ~106px recorded in July), which by itself ruled out the new typefaces as the cause. Decomposing the sections instead of measuring their totals found it immediately: **Quark 2's stock `.section` carries `padding-block: clamp(3rem, 8vw, 6rem)` - 96px top *and* 96px bottom at the 6rem ceiling, 192px per section - while live's `.modular-features` sections have 0 vertical padding.** Item 12's fix had set `padding-left`/`padding-right` on `.section` only, so the vertical padding was never touched; and because every check until now measured *one column's own* box rather than the section's padding, nothing ever pointed at it. Fixed with `.section.modular-features { padding-block: 0 }` - **scoped deliberately**, because `.section` is shared by every module and the other types are *not* the same on live (measured at 1445px: hero 120/140, text modules 15/15, modular-features 0/0), so a blanket `padding-block: 0` would flatten those too. `padding-block` is the correct property here: it leaves item 12's inline padding intact. Bumped `?v=25` → `?v=26`. **Verified:** both sections now report 0/0 padding matching live, all three icon-menu columns still share one row (`top: 1337`, no layout regression), and the test copy's total document height is now **4040px against live's 4046px** - a 6px gap where the sections alone were ~250px apart before.

  **The remaining per-column difference is now fully explained too, and it traces back to the Aug 1 audit.** With the padding fixed, each icon-menu column measures 250px against live's 276px. Decomposed: `.feature-icon` 100=100 ✓, `h6` 22=22 ✓, column padding 20/20 = 20/20 ✓, and then **24px of it is live's `<p>` carrying `margin-bottom: 24px` where the test copy's has `0`** - that is `p { margin: 0 0 1.2rem }` (1.2rem at live's 20px fluid root = 24px), **which the 2026-08-01 systematic audit had already flagged as a Tier 2 (Spectre) finding and which was never triaged.** The last 2px is the paragraph rendering at 15px/23.25px line-height on the test copy versus 16px/24px on live - the same fluid-root-font-size issue as items 9-11. **Both fixed same day - see item 22.**

  22. **Fixed 2026-09-08 (Brad's go-ahead, site-wide): paragraph size and spacing.** Measuring first changed the shape of this fix, and is worth copying as a habit: the gap was *not* a single global `p` problem. At 1445px, the test copy's **`body` font-size already matched live** (16px both), its general `p` was 16px with a **16px** bottom margin against live's **24px**, and `.modular-features .feature-content p` was separately knocked down to **15px with `margin: 0`** by Quark 2's own `.modular-features .feature-content p { font-size: 0.9375rem; line-height: 1.55; margin: 0 }`. So a bare `p` rule alone could not have fixed the icon-menu columns - that selector outranks it - and touching `body` would have been wrong entirely. Two rules, both using the established fluid-root `clamp()` technique from items 10-12 (live gets these from Spectre off its own 16→20px fluid root: `body { font-size: .8rem }`, `p { margin: 0 0 1.2rem }`): `p { margin-bottom: clamp(19.2px, 16.32px + 0.6vw, 24px) }` and `.modular-features .feature-content p { font-size: clamp(12.8px, 10.88px + 0.4vw, 16px); margin-bottom: <same clamp> }`. Bumped `?v=26` → `?v=27`.

     **Verified:** `.feature-content p` now computes 16px/24px margin and general `p` 24px margin, both matching live; the icon-menu column went from **250px to 278px against live's 276px**, and the homepage's first text section now matches live **exactly** (102px = 102px). 12-page sweep all 200, no new `grav.log` entries, screenshot clean.

     **Residual +2px per column, and the single change that would close it:** the column overshoots by exactly 2px because Quark 2's `line-height` is a uniform **1.6** (`--pico-line-height`) against live's uniform **1.5** - at 16px text that is 24.8px vs 24px per line, ×3 lines ≈ 2px. Deliberately not changed here: it is a global, one-line decision (`--pico-line-height: 1.5`) affecting every text element on the site, not part of a paragraph-sizing fix, and it wants its own go-ahead. It would also correct `li` (live 14px/21px vs test copy 16px/27.2px) and `td` line-heights in one move.

     **A warning about page-total metrics, learned here.** Before this fix the test copy's total document height was 4040px against live's 4046px - a 6px gap that looked like near-perfect parity and was **entirely coincidental cancellation**: the test copy's paragraphs were too *short* on margin (16 vs 24) while its feature columns were too *short* on height (250 vs 276), and the two errors happened to offset. After fixing both, per-element values match live but the page total moved to 4140px. **Do not use document height as a parity check on this migration** - it hides compensating errors. Per-element and per-section measurement is the only trustworthy signal, which is the same lesson items 13-16 taught in a different form.

  23. **Sponsors (`feature-images`) section root-caused 2026-09-08; three of four causes fixed.** The section measured 815px on live against 729px on the test copy. Ruled out content first - identical structure on both (8 columns, 8 images, two rows of four) and the intro paragraph byte-identical at 191 characters - then decomposed it into four named causes summing to -87 against the measured -86:

      | Cause | Live | Test copy | Contribution |
      |---|---|---|---|
      | `h2` margin-top (Quark 2 zeroes `margin-top` on all headings) | 40px | 0px | **-40px** |
      | `.column h5` font-size, driving `.columns` height | 20px | 17px | **-26px** |
      | `.frame-box > p` max-width | 600px | none (ran 960px) | **-18px** |
      | `.frame-box` padding (`1.5rem` against different roots) | 30px | 24px | **-12px** |

      **The `max-width` one is the interesting find and the familiar shape:** `.modular-features .frame-box > p { max-width: 600px; margin-right: auto; margin-left: auto }` is a **stock Quark 1 rule** (`quark/css-compiled/theme.min.css`) that Mod Quark's own `custom.css` never restates - the same blind spot as items 11/14/15/21, and again invisible to the selector-scoped audit. It is also **not only a height difference**: without it the sponsors intro paragraph rendered the full 960px instead of a centred 600px column, i.e. a visibly wider block of text that a height-only check would never have flagged. The `frame-box` padding and `h5` size are both the fluid-root `rem` trap from items 9-12 (a literal `1.5rem`/`1rem` ported across from a 20px root to a 16px one). Fixed all three, scoped to `.modular-features`, bumped `?v=27` → `?v=28`. **Verified exact:** frame-box padding 30px = 30px, paragraph width 600px = 600px (and now wrapping to three lines like live), h5 font-size 20px = 20px; grid still 4+4, 10-page sweep all 200, screenshot clean.

      **⚠️ The section total now reads 816px against live's 815px - and that 1px is COINCIDENCE, not parity.** This is the cancellation trap from item 22 happening a second time in the same session, which is why that warning is worth taking literally. The still-unfixed `h2` margin-top is **-40px**, and it is being masked by roughly **+38px** of line-height overshoot (Quark 2's uniform 1.6 against live's 1.5: the paragraph is 3 × 27.2px = 82px where live is 3 × 24px = 72px, and each `h5` is 78px against 72px, ×2 rows ≈ +22px in `.columns`). **Consequence worth planning for: fixing either one alone will make the section total look *worse* before it looks better.** They should land together, and the check afterwards must be per-element, never the total.

  24. **Landed together 2026-09-08 (Brad's go-ahead): `h2` parity + line-height 1.5. The icon-menu module now matches live EXACTLY - section 336px = 336px, column 276px = 276px.** That is the module this entire thread started from on 2026-07-23, finally closed.

      **`h2`:** live's is 1.8rem / 2rem-top / 0.9rem-bottom off its own fluid root (36px / 40px / 18px at desktop) with a 1.2 line-height, against Quark 2's 34px / 1.15 / `margin-top: 0` on all headings. Restated as clamps. **Checking every `h2` in context first was what made this safe:** live does *not* apply that 40px top margin uniformly - it shows only on the Sponsors `h2`, because live suppresses it in text modules via `.modular-text .column > :first-child { margin-top: 0 }` and in the footer via `.footer-cell h2 { margin: 0 ... }`. Both rules already exist in this file, so setting the base value reproduces live's *pattern* rather than flattening it. Verified per context after the change: modular-text 0=0, footer 0=0, modular-features 40=40, font-size and line-height 36/43.2 everywhere. Had this been applied as a blanket `h2 { margin-top: 40px }` without the context check, every text module on the site would have gained 40px live doesn't have.

      **Line-height:** setting `--pico-line-height: 1.5` was **necessary but not sufficient**, and only per-element verification caught it - Quark 2 also ships an explicit `p, li { font-size: 1rem; line-height: 1.7 }` that overrides the variable, plus `.modular-features .feature-content p { line-height: 1.55 }`. Both restated. After the variable change alone, `body` and `td` had converged while `p` and `li` were still at 27.2px; trusting the variable to propagate would have shipped a half-done fix that looked done. **Now exact against live:** body 16/24, p 16/24, td 18/27, `.feature-content p` 16/24.

      **Two new findings surfaced by this pass, neither fixed:**
      - **Quark 2 draws a decorative accent bar above every `h2`** - `h2::before`, a 28×3px near-black block with an 11.2px bottom margin (14.2px total, exactly the +14px the Sponsors `h2` still carries). **Live has no such element**, so this is a visible Quark 2 design flourish rather than a measurement artifact, and removing it is a design decision (`h2::before { display: none }`) rather than a parity bug fix.
      - **`h5` line-height is 1.3 against live's 1.2** (+6px per sponsor caption, ≈ +12px across the two rows).
      - Also still open and noted in passing: `li` font-size is 16px here against live's 14px (its line-height now matches at 1.5).

      **Sponsors section is now 849px against live's 815px (+34), and that +34 is real rather than cancelled** - it is the `h2::before` bar (+14) plus the `h5` line-height (+6 × 2 rows ≈ +19 through `.columns`). Contrast with item 23, where the total read +1 purely by coincidence. 12-page sweep all 200, screenshot clean.

  25. **Fixed 2026-09-08: `h5` line-height 1.3 → 1.2, now an exact match with live** (20px / 24px / 72px rendered height, all three identical). Set globally rather than scoped to the Sponsors captions, because Quark 2's `h5 { line-height: 1.3 }` is itself global and live's base h5 is 1.2 - confirmed in two independent contexts before generalising, a modular-text probe and the Sponsors caption. Bumped `?v=30` → `?v=31`. Sponsors section 849 → **837 against live's 815 (+22)**, icon-menu still exact at 336, grid still 4+4, 13-page sweep all 200, no new log entries. Of the remaining +22, **+14 is the `h2::before` accent bar from item 24** (Quark 2 design element, live has no equivalent - awaiting a decision, not a bug) leaving roughly 8px unattributed.

      **Found while scoping this, and more consequential than the `h5` fix itself: `h3` has the same problem and is far more widespread.** On `/about-us/history` alone there are **20 `h3` elements**, rendering at **24px / 1.25** against live's **28px / 1.20** - whereas `h5` does not appear on that page at all. Deliberately not fixed here: unlike `h5`, correcting `h3` changes font-size as well as line-height (24px → 28px, i.e. live's 1.4rem off its fluid root), which is a visible type-scale change across the site's most heading-dense pages rather than a spacing tweak. **This is the natural next item.** Worth re-probing `h4`/`h6` in the same pass - live's headings are uniformly 1.2 line-height while Quark 2 mixes 1.15 and 1.3, so `h4` is likely wrong too and simply absent from the pages measured so far.

  26. **Fixed 2026-09-08: `h3` parity - and a correction to item 24's `h2` fix that only a SECOND page revealed.** Bumped `?v=31` → `?v=32`.

      **The lesson first, because it is the reusable part: verifying a fix on one page is not enough.** Item 24 reported `h2` as matching live exactly, and on the homepage it genuinely did (modular-text 0=0, footer 0=0, features 40=40). Measuring `/about-us/history` showed 25 of its 28 `h2` elements at **59.4px** top margin against live's flat 40px. The cause: **Quark 2 ships a contextual heading-rhythm system that live has no equivalent for** -
      ```css
      :is(p,ul,ol,dl,blockquote,pre,table,figure,hr,iframe,.card) + h2 { margin-top: 1.65em }  /* 1.65 x 36px = 59.4px */
      :is(p,ul,ol,dl,blockquote,pre,table,figure,hr,iframe,.card) + h3 { margin-top: 1.55em }
      :is(h1,h2,h3) + :is(h1,h2,h3) { margin-top: 0.25em }
      ```
      all at specificity **(0,0,2)**, which silently beats a bare `h2` **(0,0,1)**. So item 24's rule only ever won where a heading had **no preceding sibling** - which is exactly the case on the homepage, where the Sponsors `h2` is a first child. Live applies a flat 2rem to every heading regardless of context, so those selectors are now restated at matching specificity (later source order wins) rather than left to lose. **Deliberately not extended to `h1`:** the hero `h1`'s position is already verified against live (item 3) and is entangled with h1 spacing, so it was left untouched rather than risked for tidiness. `.modular-text .column > :first-child { margin-top: 0 }` still overrides all of it at (0,2,0), preserving live's suppression in text modules.

      **`h3`:** live is 1.4rem / 2rem-top / 0.7rem-bottom off its fluid root (28px / 40px / 14px), weight 500, line-height 1.2 - **uniform across all 20 instances** on `/about-us/history`, no context variation. Quark 2 shipped 24px / 1.25 / weight 600 / margin-top 0. **Font-weight was a third gap neither of us had noticed**: live's `h2` and `h3` are weight **500** where Quark 2 ships **700** and **600** - visibly bolder headings across the whole site, caught only because the per-instance signature included weight.

      **Verified on both pages.** `/about-us/history`: all 20 `h3` now `28px|33.6px|mt40|mb14|w500` = live exactly; all 25 body `h2` now `mt40|w500` = live exactly; `h6` already exact. Homepage: modular-text `h2` mt0 = live, features `h2` mt40 = live, icon-menu section still exact at 336px. 13-page sweep all 200.

      **Still open after this:** the footer `h2` margin-bottom is 8px against live's 10px (small, pre-existing, unrelated to this pass); `li` font-size 16px vs live's 14px; and the Sponsors section's remaining +22px, of which +14 is the `h2::before` accent-bar decision and roughly 8px is still unattributed.

  27. **Fixed 2026-09-08: `h4` parity - found by scanning all 76 sitemap pages rather than guessing.** `h4` turns out to exist on **exactly one page site-wide** (`/bunnybots`, four instances), which is why every earlier heading pass missed it: it simply is not on the homepage or `/about-us/history`. It was wrong on every property - live `24px / 28.8px / mt40 / mb12 / weight 500` against Quark 2's `19px / 24.7px / mt27.55 / mb7.6 / weight 600`, with that 27.55px being `1.45em × 19px` from the same contextual rhythm rule as item 26. Restated as clamps off live's fluid root (1.2rem / 2rem / 0.6rem) and `h4` added to the contextual-override selector list. Bumped `?v=32` → `?v=33`. **Verified:** all four now `24px|28.8px|mt40px|mb12px|w500` = live exactly; `h2`/`h3` on the same page unchanged and still exact (a useful third-page confirmation that items 24/26 generalise); 14-page sweep all 200.

      **The scan is the reusable part** - `curl` each sitemap URL on both sites and count `<hN` occurrences, then only open the browser for pages where counts or tags differ. It is far cheaper than driving a browser across 76 pages and it is what surfaced both this and the blog-title finding below.

  28. **DEFERRED by Brad 2026-09-08, deliberately not a bug: the three history blog pages render their entry titles as a different heading level.** The same scan found `/about-us/history/history-2019-blog` (34), `/about-us/history/history-2022-23-blog` (7) and `/about-us/history/history-2024-25-newsletters` (1) - **42 headings** - rendering as `<h5 class="p-name mt-1">` on live but `<h3 class="p-name">` on the test copy. Not missing content (all 200, comparable byte size); a **template** difference, from `partials/blog/title.html.twig`, which **neither child theme overrides** - so each simply inherits its own parent's version (quark vs quark2). This is the first difference in this migration that comes from an un-ported *parent-theme template* rather than a missing CSS property, and it changes the document heading outline (screen readers, search engines) as well as the visual size - so it is not purely cosmetic.

      **Brad's decision: leave these alone for now.** The reason, worth recording so this is not re-opened as a defect by a later session: **these pages are from a much older version of the website**, and how to handle them is a content decision the team needs to make separately - not something the theme port should quietly normalise. Revisit when that decision is made; the fix, if one is ever wanted, is to override `templates/partials/blog/title.html.twig` in `mod-quark-2` with live's version.

     **Remaining per-section deltas after this fix, at 1445px** (measured, and partly entangled with the known content drift, so read them with that caveat): hero 812=812 ✓, text#1 102=102 ✓, text#2 235→251 (+16), icon-menu 336→326 (−10), text#3 427→425 (−2, and live has **7** paragraphs there against the test copy's **6** - content drift, not CSS), text#4 304→349 (+45, a YouTube-embed module with zero text on both, so it is iframe sizing rather than typography), **Sponsors 815→729 (−86, the largest and least explained - the `feature-images` module, never investigated).** The Sponsors −86 is the natural next thing to look at.

**Two things this session surfaced that need Brad, not Claude:**
- **Ownership drift - RESOLVED 2026-09-08.** Four files (`base.html.twig`, `gallery-press.html.twig`, `gallery-press.yaml`, `sponsorship-levels.css`) landed as `brad:editor` rather than `grav:editor` - a plain `scp`/`cp` as `brad` creates files under his own ownership, so **expect this after any file you add to the test copy this way, and check afterwards**. Brad ran `sudo chown grav:editor` on all four; verified `grav:editor 664`, site still 200. A follow-up sweep (`find ... ! -user grav -o ! -group editor`) shows every remaining non-conforming file is an inert `base.html.twig.bak-vNN` backup (v12-v22, 11 of them) that Grav never reads - harmless, but worth deleting during Phase 5 cleanup so they don't ride along at cutover.
- **Content staleness of the test copy:** its `user/pages` is July-vintage - **48 of live's 252 pages have changed since 2026-07-22, and the test copy has 239 pages to live's 252.** This does *not* affect the theme port (at cutover, live's real pages stay put and only the theme swaps), but it *does* add noise to any live-vs-copy screenshot comparison, and it's why the sponsor-logo count differs. Only `/community/summer-classes` was content-refreshed this session (needed to exercise gallery-press; its stale `default.md` was renamed to `default.md.bak-presync-20260908` since live has only `modular.md` there and both together make template resolution ambiguous). Decide before the next comparison pass whether to re-mirror pages wholesale or keep treating content diffs as expected noise.

**Structural note, worth a decision rather than another workaround:** this migration has now been in flight ~7 weeks against an actively developed live site, and this is the second time work has been done against a baseline that moved underneath it. Options: freeze live theme changes during a final push, accept a mandatory re-sync immediately before cutover, or reconsider the timing. Brad's call.

**Phase 3 is still NOT complete.** Nav/hero/pill/typography (items 1-10) are solid, verified repeatedly. Icon-menu/Sponsors grid mechanism (13) is solid, verified by checking all 3 columns' positions. Icon-menu/Sponsors icon colors and heading styling (16) now fixed and verified. Icon-menu/Sponsors *exact sizing* (11, 12, 14, 15) is close but still has the unexplained section-height residual noted above - `#header`'s own sizing gap (found by the audit, not yet fixed) is a plausible contributor and should be checked first next session. The systematic audit has now run once, but its own Tier 2 (Spectre) findings aren't fully triaged, and - more importantly - **it only ever covers selectors live's custom.css already touches, so it structurally cannot find gaps like item 16's icon colors on its own; screenshot comparison is still required, not optional.** There are very likely more instances of this same pattern waiting in other custom modules (footer, galleries, text/year-modules) that haven't been checked at all yet, since all discovery so far has been on the homepage's icon-menu/Sponsors specifically. Do not tell Brad this is "done" again until the full page list has been screenshot-compared, not just the homepage.

**Read the Phase 3a re-sync entry above before resuming any of that.** Everything in items 1-16 was calibrated against live's *pre-2026-09-04* typography. The section-height residual has since been re-baselined and **root-caused - see item 21; it was Quark 2's stock `.section padding-block`, not typography at all** - which also means the re-baselining exercise is done and the `#header` sizing gap is the one still measured against stale numbers. Remaining list, in priority order:
  1. **Headings h2-h6 are now all verified exact against live** (items 24-27), across the homepage, `/about-us/history` and `/bunnybots`. What has *not* been checked is the rest of the body typography on those same pages - `blockquote`, tables, lists in context, form elements. Use the item-27 curl scan to find which pages actually contain each, then measure only those.
  2. Small known gaps: footer `h2` margin-bottom 8px vs live's 10px; `li` font-size 16px vs live's 14px; Sponsors' remaining +22px (+14 of it the `h2::before` accent-bar **design decision** - live has no such element - and ~8px unattributed).
  3. Deferred by Brad, not defects: the blog-title heading level (item 28) and the `h2::before` accent bar.
  3. **`#header` sizing** - still open, and still calibrated against pre-Sept-4 numbers, so re-measure before chasing.
  3. **The rest of the untriaged Tier 2 (Spectre) findings** - `.button` family, `body`/`h1`/`blockquote` base typography, `.float-left`/`.float-right`.
  4. **The `body.no-hero #start` decision** from Phase 3a.
  5. **Modules and pages never screenshot-compared at all** - footer, galleries, text/year-modules, and everything beyond the homepage.

**Unrelated pre-existing bug found, confirmed NOT a migration issue (present on unmodified live too):** admin2's own Page Media preview thumbnails don't render in the page editor (e.g. `/admin/pages/edit/home/_gallery` - broken-image icons instead of thumbnails). Verified the actual files exist on disk with correct `grav:editor` permissions, nginx isn't blocking them, disk space is fine (6.4G free), GD/Imagick both loaded, nothing image-related in `grav.log`. **The public site is completely unaffected** - the real front-end image URL (`/images/.../hash-filename.jpeg`) returns a valid 200 with real image bytes. Admin2's own internal preview mechanism only, cosmetic, not blocking anything. Initially suspected `custom_base_url` (below) but that's disproven - live has the identical broken thumbnails with `custom_base_url` pointed correctly at its own domain, so that config was never the cause.

**Test-copy-only config change (not a fix for the above, kept anyway):** `system.yaml`'s `custom_base_url: 'https://robot.mbhs.edu'` was commented out on the test copy only (`/srv/robot-grav-site-quark2/user/config/system.yaml`, backup alongside as `system.yaml.bak-quark2test`) since it's more correct for a non-standard loopback-origin test environment regardless of the thumbnail investigation's outcome. Live's own `system.yaml` is untouched. Revert if ever needed for byte-parity with live's config.

### Phase 4 - Cutover
- [ ] Low-traffic overnight window, fresh backup first.
- [ ] Directory swap (not in-place), same as the 2.0 core cutover.
- [ ] Re-run the Phase 3 checklist against the public URL.
- [ ] Confirm `.git`, `backup.sh`, `.user.ini`, `robots.txt`, the GSC file, and the `/api/v1/sync` 403 block all survived the swap.

### Phase 5 - Soak and cleanup
- [ ] Keep the Quark-1 archive about 1 week before removing anything.
- [ ] Update this doc's Key File Paths / Architecture reference for the new theme.
- [ ] Log the final outcome (shipped / abandoned / deferred) in CHANGELOG.md and Changes.md.
- [ ] Retire the old `mod-quark` and Quark 1 parent theme directories.

**Rollback:** the Phase 4 directory swap makes rollback a one-line `mv` back to the archived Quark-1 tree - same as every other cutover on this site.

**Effort:** closer in scope to the Grav 2.0 core migration than a routine CSS tweak - a dozen-plus templates/partials (full corrected list in the Phase 0 findings above) plus a full `custom.css` audit. Plan for a between-events window, not mid-season.

---

## Key File Paths Reference

| What (live `/srv/robot-grav-site/`) | Path |
|------|------|
| Active theme CSS | `user/themes/mod-quark/css/` |
| Theme blueprints (custom modules) | `user/themes/mod-quark/blueprints/` |
| Theme config | `user/config/themes/mod-quark.yaml` |
| System / site config | `user/config/system.yaml`, `user/config/site.yaml` |
| Plugin configs | `user/config/plugins/` |
| User accounts | `user/accounts/` |
| Page content | `user/pages/` |
| Grav CLI / package manager | `bin/grav`, `bin/gpm` (run as grav: `sudo -u grav php bin/grav ...`) |
| Cache-bust version | `?v=NN` in `templates/partials/base.html.twig` |
| Activity log script (nightly diff → `ACTIVITY.md`) | `/srv/scripts/activity-log.sh` (cron: `brad`, `15 3 * * *` UTC) |
| Activity log output | `/srv/scripts/activity-log.log` |
| Activity log shadow copies (diff baseline) | `/srv/.activity-shadow/` |
| Docs-repo clone (pushes `ACTIVITY.md` to this repo) | `/srv/robot-grav-site-docs/` |
| Docs-repo deploy key | `~brad/.ssh/robot_grav_site_docs_deploy` (SSH config alias `github-robot-grav-site-docs`) |
| Git Sync local repo | `user/.git` (pushes to `robot-grav-site-sync` on GitHub) |
| Retired whole-site git repo (inert, kept for reference) | `.git.retired-20260727` (Grav root) |
| PHP config (live) | `/etc/php/8.3/fpm/php.ini` |
| nginx config (live) | `/etc/nginx/sites-available/grav` |

---

## People & Roles

| Person | Role |
|--------|------|
| Rafi Pedersen | Site admin, server/infrastructure owner. Contact for live-server system-level recovery. |
| Brad (comms@navybook.com) | Project lead. Editor + Onboarder + Admin on live; super-admin access. SSH access to the live server. |
| James P | Previously rescued the site after a PHP update broke it — reference for system-level issues. |

---

## Standing constraint — BlairMdITC font (license-gated, OFF)

`custom.css` has BlairMdITC's `@font-face` and the `h1` font-family rule **commented out**. The font is a commercial ITC/Monotype **desktop** font — a desktop license doesn't cover web `@font-face` serving. **Future improvement, not currently planned:** if the team ever wants to enable it, purchase an ITC Blair Medium **webfont** license (MyFonts/Monotype, ~$30-60 lowest tier; register to the team, keep the receipt/EULA), then uncomment the two spots and ship the font files to `user/themes/mod-quark/fonts/`.
